# 確認・修正UIへのフィードバック（2026-08-26・ユーザー実使用の所感）

**2026-08-26 実装済み（v0.3.0）: 全項目（1〜7）実装完了。**
参考実装はすべて SENSEKI FEVER（`C:\Users\iftec\Documents\mario-tennis-records`）にある。

実装メモ（v0.3.0）:
- 7: `vision.js` matchGlyph がラベル別候補ランキングを返すようになり、`index.html` の
  `validateWinnerScores` が「7対0〜5/8対6」制約で読み直す（候補FLOOR=0.5・置換は[自動補正]表示＋赤枠・
  不成立なら信頼度の高い側だけ残して他方は空欄）。修正UIのスコア入力にもFEVER式補完（8→6等・result自動）あり。
- 2/3: `data-combo` 属性のプルダウン（char/court/racket）。かな正規化部分一致・↑↓Enter・手入力可。
- 1: 既定ビューが「1件ずつ確認」。Enter=次へ/Shift+Enter=前へ。「一覧表示」トグルで俯瞰。
- 4: 手修正セルは青枠（.field.edited）。基準値はレート連鎖補完後のスナップショット（m._orig）。
- 5: 案A＋案Bの両方を実装。案A=CSV保存時に確定名を履歴へ収穫（userData/names-user.json）し、
  名前入力欄のプルダウンに直近対戦順で最大5件。案B=名前クロップのtextVecを保存済みテンプレと
  nccWh照合（白chのみ・閾値0.90）し一致なら自動入力＋[自動入力]表示。手で直すとマークが外れ、
  修正後の名前で再収穫される（教えるサイクル）。設定チェックボックス（userData/settings.json）で
  自動入力ON/OFF可・履歴プルダウンは常時有効。自動入力のまま確定した名前はテンプレ再登録しない・
  同名テンプレは直近3件まで・10文字超は収穫しない（ニックネーム上限）。

## 1. 1件ずつ確認できるビュー（レビュー画面）
現状は全試合の一覧のみで1試合がコンパクトすぎて見にくい。
**一覧とは別に「1試合ずつ大きく表示するビュー」**を追加し、ボタンまたは **Enter で次の試合へ**進める。
チェック作業の主動線はこちらにする（一覧は俯瞰用として残す）。

## 2. キャラクター入力をプルダウン＋部分一致に
手入力はミスが出る。**プルダウン選択＋手入力両対応・入力中は部分一致で候補表示**。
参考: `mario-tennis-records/components/SearchSelect.tsx`（MatchForm でキャラ/ラケット/コートに使用。
options[].name 表示・プレビュー画像つき。候補ボタンの省略名ヘルパーもある）。

## 3. コートも同じ仕様のプルダウンに
キャラと同じ SearchSelect 方式で。

## 4. ユーザーが修正したセルのハイライト
自信が無いセルを赤枠にしているのと同様に、**ユーザーが手で直したセルは別色でハイライト**
（どこを直したかが後から分かる）。CSV保存前の最終確認にも効く。

## 5. プレイヤー名の自動入力（未実装・要望として正式化）
現状は名前のクロップ画像を見て手入力する方式で、**自動入力は未実装**。
実装するなら（ユーザー指定: **自動入力の有無を設定で選べる**こと）:
- 案A: FEVER の `components/OpponentNameInput.tsx` 方式 — 過去の対戦相手名を直近対戦順で候補表示（updated_at 降順・最大5件）
- 案B: 名前クロップ画像を過去に確定した名前テンプレと照合して自動補完（myname と同じ textVec 照合。
  教えるUIで「この画像=この名前」が蓄積されれば実現可能）
自由文字列のOCRは辞書方式と違い難度が高いので、A→B の順が現実的。

## 6. 証拠画像1枚目（対戦カード全景）の目的
VS画面のフルショット。**コート名・両者のキャラ・プレイヤー名を1枚で確認する**ための証拠画像
（個別クロップだと文脈が消えるため全景を残している）。ビュー刷新時もこの役割は維持する。

## 7. スコアの妥当性検証を追加（4→6誤読の網・最重要）
「7-6」はありえないのに赤枠にならなかった実例あり（4→6誤読・通算6例目）。
**FEVER の isValidScore と同じ規則を検証に追加する**:

```
有効な最終スコアは「7 対 0〜5」か「8 対 6」だけ
（mario-tennis-records/components/MatchForm.tsx の isValidScore・L421〜427）
```

**重要（ユーザー指示）: 無効なスコアはフラグを立てるだけでなく、そのまま表示しないこと。**
ありえない数字と分かっているのだから、**妥当性制約の下で読み直して値を置き換えてから表示**する:
1. readGlyphs/parseNumber は各桁のテンプレ照合スコアを持っている。1位の解釈が無効なスコアを
   作るなら、**2位以下の候補のうち有効なスコアになる読みを採用**する
   （例: 7-6 → 相手側は 0〜5 のどれか。「6」の次点候補が「4」なら 7-4 に置き換える）。
2. 片方が高信頼なら isValidScore の補完規則で他方を絞り込める（7→相手は0〜5 / 8→相手は6 / 6→相手は8）。
3. 置き換えた値には必ず要確認フラグを付ける（自動補正であることが分かるように）。
4. どの候補でも有効にならない場合だけ空欄＋フラグ（**無効な数字を出力に残すのは禁止**）。
数字テンプレの根本対策（lightink「4」）の前に、この制約付き読み直しだけでも入れる価値が高い。
FEVER側には「片方を入れるともう片方を自動補完する」UX（8→6・6→8・≤5→7）もあるので、
修正UIでも同じ補完を入れると入力が速くなる。

## 実装順の提案
7（スコア検証・数行で効果大）→ 2/3（SearchSelect移植）→ 1（1件ビュー）→ 4（修正ハイライト）→ 5（名前補完）
