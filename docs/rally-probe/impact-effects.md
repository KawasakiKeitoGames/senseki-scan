# 打点エフェクトの網羅ととびつきショット

調査 2026-08-25。担当: 打点（ラケット接触）に出るエフェクトの網羅と、**出ないケース**の洗い出し。
実フレームを 1/60 秒刻みで目視 ＋ 画素値実測。推測は「未確認」節に隔離。

作業ファイル: `C:/Users/iftec/AppData/Local/Temp/claude/C--Users-iftec-Documents-Claude/61bc9629-efb3-474e-aaa8-2e0d1cbe769e/scratchpad/h-impact-effects`
（`an.js`＝ボール追跡＋白画素、`ring4.js`＝参照フレーム不要の円形リング検出器、各種タイル PNG）

---

## 判定

**「打点」と「バウンド」は見た目で区別できる。ただし単純な明るさ閾値ひとつでは不可。**

1. **バウンドには一切エフェクトが出ない。** 砂 x2・ウッド x1 の実バウンドを 1/60 秒ずつ拡大目視した結果、
   砂埃も、白リングも、水しぶきも、星形バーストも、**何も出ない**。
   見えるのは「ボールが自分の影に接触する」ことだけ。地面のテクスチャも変化しない。
2. **打点には必ず「拡大する円環（リング）」が出る。** 11例すべてで出た。
   リングは**打点位置にほぼ固定されたまま半径だけ広がる**（ボールは離れていく）。
   持続 0.15〜0.25秒。**エフェクト無しの打点は 0/11**。
3. **ただしリングの強度は 3桁の幅がある。**
   通常ショット＝塗りつぶしの巨大リング（画面が飛ぶ）／
   **とびつき・守備的ロブ・弱い返球＝線幅 2〜3px の極細の白い環（3/11）**。
   後者は「ショットのエフェクト（色つきトレイル・破裂）」が**本当に一切無い**。ユーザー指摘どおり。
4. **「白い4条星フラッシュ」は打点マーカーではない。**
   マックスチャージ**完了**の合図で、打点との時間差は **−0.25秒 〜 +0.15秒** と実測でバラつく（5例）。
   チャージキャンセルもできるので、フラッシュから打点も球種も決まらない。
5. **参照フレーム差分は使えない。** カメラのドリーは実測で **5px/frame（FHD）** 出る。
   ウッドコートの明暗の板目 × カメラ移動で、差分ベースの検出器は**バウンドを 100% 誤検出**した。
   → **同一フレーム内で完結する検出器**（コート輝度の85パーセンタイル基準）にする必要がある。

---

## A. ユーザー提供の正解実例（`2026-08-20 10-44-35.mp4` 386.0〜388.2秒 / 砂コート / 1080p60）

### A-0. まず時刻の訂正（重要）

ユーザー資料は「t≈386.80 がゲッソーの接触点（FHD 696,332）」としているが、
**実フレームでラケットとボールが接触するのは t=386.400（FHD 776,235）**。
386.80 の (696,332) は**打った後の飛行中の一点**。

追跡実測（960空間の重心 → FHD換算。`an.js`）:

| t | ボール(FHD) | 備考 |
|---|---|---|
| 386.350 | (781,251) | ラケットへ向かって上昇中（影が下に見える） |
| 386.383 | (781,239) | ガット直前 |
| **386.400** | **(781,235)** | **接触。目視でボールがガットに接している／y の反転点** |
| 386.417 | — | 振り抜きモーションブラーでボール消失 |
| 386.583 | (769,252) | 下降開始 |
| 386.783 | (696,333) | ← ユーザー資料の「打点」の位置 |
| 387.000 | (565,500) | |
| **387.217** | **(454,812)** | **バウンド（影と接触）** ← ユーザー資料と一致・正しい |
| 387.400 | (447,846) | ほぼ転がる（ドロップなので跳ねが死ぬ） |
| **387.550** | ≈(390,800) | **ハナチャンのとびつき接触** |

- x速度が接触前 ≈0px/frame → 接触後 −5〜−12px/frame と**不連続に変化**。運動学的にも 386.400 が打点。
- 目視根拠: `g3_tile.png`（386.350〜386.533 を 2倍拡大した 4x3 タイル）。
  386.400 のコマでボールがガットに接し、386.433 のコマから白い円盤が出る。

### A-1. マックスチャージ完了フラッシュ（打点の 0.25〜0.12秒前・キャンセルされた）

`386.150 〜 386.283`（**打点の −0.250 〜 −0.117秒**、約15フレーム）に
**画面を覆う巨大な白い4条星**がゲッソーの胴体に重なって出る（中心＝キャラ、ボールではない）。

- ピーク t=386.167。**pureWhite画素数（960空間・4px間引き・L>238 かつ max−min<26）が 327 → 783（2.4倍）**。
- 減衰: 386.183(733) → 386.200(619) → 386.233(466) → 386.283(329＝ベースライン)。
- この15フレームの間、**ボールは (765,260) 付近に浮いたままでまだ打たれていない**。
- そのあと **386.300〜386.383 は完全に無エフェクト**（構え直し＝チャージキャンセル）。
- → **「白フラッシュ →（0.12〜0.25秒の無）→ 打点」という並びが実在する。**

### A-2. 打点エフェクト（ドロップショット・チャージキャンセル後の通常打）

t0 = 386.400。

| 相対時刻 | フレーム | 見えるもの |
|---|---|---|
| t0 −0.05 〜 t0 | −3〜0 | **無エフェクト**。ボールが影を伴ってラケット面へ入る |
| t0 +0.017 | +1 | ラケットの振り抜きモーションブラー（白い弧） |
| **t0 +0.033** | **+2** | **打点に小さな白い円盤（直径 ≈20px FHD）が出現** |
| t0 +0.05 〜 +0.13 | +3〜+8 | 円盤が拡大。**放射状の白い短線（時計の目盛り状）**を伴う。直径 ≈40→80px |
| t0 +0.15 〜 +0.18 | +9〜+11 | **最大**。直径 ≈130〜150px FHD。ほぼ真っ白に飛ぶ |
| t0 +0.10 〜 +0.28 | +6〜+17 | **紫〜藤色の三角形の破片**が放射状に 8〜12個 飛ぶ |
| t0 +0.18 〜 +0.50 | +11〜+30 | 環が消え、**白い彗星状トレイル**（＝ドロップショットの白トレイル）がボールを追う |

- pureWhite はこの区間で 559〜583（ベースライン 320 の **1.75倍**）。
  **マックスチャージ星（2.4倍）より小さい**。リングは半透明で L>238 に届かない画素が多い。
  → **pureWhite だけで打点を測るとリングを取りこぼす。**

### A-3. バウンド（t=387.217・砂コート）★最重要

`387.150〜387.333` を 240x200 → 2倍拡大（`bz_tile.png`）で 1/60 秒ずつ目視。

**エフェクトは一切ない。**

- ボールと**楕円の影**が近づき、387.217 で接触（影とボール下端が重なる）。
- 跳ね上がらず、387.23〜387.55 の 0.3秒間、影の直上を**転がるように**進む。
- 砂の表面テクスチャは**まったく変化しない**（バウンド痕も残らない）。

### A-4. とびつきショット（ハナチャン・t≈387.550）★本命

**とびつきモーションの見た目**
- 387.40 頃には**すでに地面に伏せて左へ滑っている**。「横に飛ぶ」より **腹ばいのスライディング**。
  体の全節が横倒しで砂の上を滑り、白い靴が体の後ろに流れる。
- ラケットは**体の左前方に伸ばしきり**、ラケット面が地面すれすれ。
- 打った後も**起き上がらず 0.3秒以上伏臥**（387.6〜387.9）。

**エフェクト**（`hz2_tile.png` = 300x240 を 2倍拡大）

| 相対時刻 | 見えるもの |
|---|---|
| t0 −0.017（387.533） | ボールが地面すれすれ、ラケットの左。**無エフェクト** |
| **t0 = 387.550** | ラケット面が接触。**ラケット下に白い微細な粒（スパーク）が5〜8個** |
| t0 +0.017 | 粒が増え、細い弧が現れ始める |
| t0 +0.033 | **極細の白い円環**（線幅 2〜3px FHD）がラケットヘッドを中心に出現 |
| t0 +0.050 | 円環が拡大（直径 ≈160px FHD・**線幅 ≈19px FHD**）。最も見やすい |
| t0 +0.067 | さらに拡大し急速に薄くなる |
| t0 +0.083 以降 | 消滅。**以後 0.3秒間、完全に無エフェクト** |

**画素実測**（1920x1080実座標・y=852 の水平走査 x=360〜560）:

| | RGB | L | S |
|---|---|---|---|
| t=387.500（環なし）コート面 | (219,203,100) | 197 | 0.54 |
| **t=387.600 環の芯** | **(228,228,228)** | **228** | **0.00** |
| t=387.600 環のすぐ外側 | (213,193,90) | 187 | 0.58 |

→ 環は**完全な無彩色 (228,228,228)**。ΔL = **+41**、ΔS = **−0.58**。
→ ただし **コートラインの実測 (238,225,223) L=229 S=0.06（shot-color.md G項）とほぼ同じ**。
  **色だけではラインと区別できない。円弧であること・動くことが決め手。**

**結論（とびつき）**
1. ユーザーの言う「ショットのエフェクトはなし」は**正しい**。
   **色付きトレイル（青/紫/赤/黄/白の彗星）も破裂も一切出ない**。返球されたボールは無地。
2. ただし **「打点そのものを示す白い細環」は出る**。通常打の巨大リングの縮小・希薄版。
   塗り面積比でおよそ **1/30〜1/100**。
3. リング検出器（下記E項）では **68%**（打点しきい 35〜40%）で拾えた。**取りこぼさない。**

---

## B. 通常の打点に出るエフェクトの分類（11例・7コート）

### B-1. 実例一覧（すべて実フレームを目視）

| # | 動画 | 打点時刻 | コート | 種別 | 打点エフェクト |
|---|---|---|---|---|---|
| 1 | 10-44-35 | **386.400** | 砂 | ドロップ（チャージキャンセル後） | 白い拡大円盤＋放射目盛り＋紫の破片 |
| 2 | 10-44-35 | **387.550** | 砂 | **とびつき** | **細い白環＋微細スパークのみ** |
| 3 | 22-02-04 | **118.583** | クレイ赤（近） | 青スライス・**マックスチャージ** | 巨大白4条星 → 白→水色→青→紫の拡大リング＋青ビーム |
| 4 | 22-02-04 | **119.717** | クレイ赤（遠） | 紫フラット | マゼンタの拡大リング＋マゼンタビーム |
| 5 | 00-57-56 | **290.883** | 青ハード | 水色スライス | **白い星芯入りの水色リング** → 水色の破片 → 水色ビーム。UIに「GOOD」 |
| 6 | 00-57-56 | **292.300** | 青ハード | 守備的ロブ（低い打点） | **細い白環＋白スパークのみ**（＝とびつき型） |
| 7 | 01-57-16 | **304.363** | 石畳 | 橙トップスピン | 白→黄→橙→赤の拡大リング（外周に炎状の棘） |
| 8 | 01-57-16 | **245.100** | 石畳 | 黄ロブ（低い打点） | **淡い黄緑の弱い環**（＋足元の白い砂煙はキャラの足運び由来） |
| 9 | 00-39-45 | **501.670** | ウッド | ボレー（ノーバウンド） | 金色の非常に明るい拡大リング |
| 10 | 00-57-56 | **113.050** | 船・木甲板 | ボレー | 白い放射目盛り入りの円盤 |
| 11 | 22-18-37 | **302.100** | クレイ赤 | **地面すれすれの横伸ばし返球** | 白い花状スパーク → 金色の巨大リング |

**★ 実例 11 は「バウンドと最も紛らわしい打点」の見本**（`cy_tile.png` / `cw_tile.png`）。
ボールが影と重なるほど低い位置でノコノコが左に伸ばして拾っている。
接地点そのものから白い花状のスパークが出て、そのあと金色のリングが巨大化する。
**幾何だけ見ればバウンドと区別できない**が、
(a) **x 速度も反転している**（接触前 −1.2px/frame → 接触後 +11.4px/frame）、
(b) **リングが出る**、の2点で打点と分かる。

### B-2. 3要素と、打点フレームからの相対時刻

**(a) インパクトリング／円盤（＝打点そのものの印）— 11/11 で出現**

| 項目 | 実測 |
|---|---|
| 出現 | **打点 +1〜+3 フレーム（+0.017〜+0.05秒）** |
| 最大 | **打点 +8〜+12 フレーム（+0.13〜+0.20秒）** |
| 消滅 | **打点 +15〜+22 フレーム（+0.25〜+0.37秒）** |
| 中心 | **打点位置に固定**。寿命中の移動は ≤40px（同時間にボールは 100〜300px 移動） |
| 最大半径 | **≈ (3.5〜5.5)× ボール直径 d**。d = 0.0155×(y+651)（FHD） |
| 拡大速度 | 通常打 **6〜10 px/frame**（＝0.5〜0.7 d/frame）／とびつき型 **3〜4 px/frame** |
| 色 | **出た瞬間は白**。その後 **ショット色へ寿命変化**（白→水色→青→紫 ／ 白→黄→橙→赤 ／ 白→マゼンタ）|

→ **色は分類に使えない**（shot-color.md B-2 の結論と一致）。**位置と形と時刻だけを使う。**

**(b) 白い振り抜き弧（ラケットのスウィッシュ）— 打点 −0.05 〜 +0.05秒**
- ラケットの軌跡そのもの（モーションブラー）。**打点の直前から出る**ので、
  「エフェクトが出た＝もう打った」ではない点に注意。
- 弧は**細長い三日月**で、円ではない。リング検出器の円形性テストで自然に落ちる。

**(c) 打球トレイルの尾端（＝打点）— 打点 +0.02 〜 +0.50秒**
- shot-color.md H の実測どおり、19〜29フレーム（0.32〜0.48秒）残る。
- **尾端が打点**。ただし**とびつき・弱い返球ではトレイル自体が出ない**（実例 2・6）。
  → **トレイル尾端は打点の必要条件ではない。**

### B-3. マックスチャージ白フラッシュの実測（5例）— 打点マーカーにならない証拠

pureWhite画素数（960空間・4px間引き・L>238 かつ max−min<26）:

| 動画 | フラッシュ区間 | 打点 | **打点との差** | ベース→ピーク | 倍率 |
|---|---|---|---|---|---|
| 10-44-35 | 386.150〜386.283 | 386.400 | **−0.250 〜 −0.117**（キャンセル） | 327→783 | 2.4x |
| 22-02-04 | 118.567〜118.700 | 118.583 | **−0.017 〜 +0.117** | 750→1652 | 2.2x |
| 00-39-45 | 500.233〜500.317 | ≈500.22 | **+0.01 〜 +0.10** | 405→1050 | 2.6x |
| 01-57-16 | 246.200〜246.283 | ≈246.30 | **−0.10 〜 −0.02** | 330→899 | 2.7x |
| 00-57-56 | 328.950〜329.050 | 328.900（ボレー） | **+0.05 〜 +0.15** | 490→1008 | 2.1x |

→ **時間差は −0.25 〜 +0.15秒 とバラつく**。フラッシュ＝チャージ完了の合図であって打点ではない。
→ 倍率は 2.1〜2.7倍と安定しているので **「マックスチャージが完了した」の検出には使える**
  （ただし**打たれたか／キャンセルされたかは分からない**）。

### B-4. その他、打点と紛らわしいエフェクト（誤検出源）

| 現象 | 実例 | 特徴 |
|---|---|---|
| **チャージオーラ（虹色のプリズム粒子）** | 22-02-04 t=120.14〜120.45（ゲッソー） | キャラの周囲に 1秒以上。**円環ではなく雲状**。ball.js が n=250〜320 の塊として拾い、追跡が乗り移る |
| **地面の黄色い星マーカー** | 22-02-04 t=118.48〜118.55 | 落下予定地に描かれる。**打った瞬間に消える** |
| **キャラ固有の常時エフェクト** | ゲッソーの足元の水色の水たまり（22-02-04 全体） | 常時。動かない |
| **足運びの砂煙／白い粉** | 01-57-16 t=245.10 のピーチの足元、00-39-45 のヨッシー足元 | 打点ではなく**足元**に出る。打点より **50〜120px 下** |
| **UI「GOOD」表示** | 00-57-56 t=290.9〜291.3 | タイミング良好時のみ。画面固定位置のテキスト |
| **ボール自身のトレイル** | 全動画 | 細長い帯。**打点位置を通過する**ので円形性テストが要る |

---

## C. エフェクトが出ない／弱いケース（「エフェクト無しの打点」の割合）

### C-1. 実測した 11例での内訳

| 分類 | 件数 | 内容 |
|---|---|---|
| **完全に無エフェクト** | **0 / 11** | 該当なし。**どの打点にも最低限リングは出た** |
| **色つきエフェクトが無い（細い白環のみ）** | **3 / 11** | ①とびつき（10-44-35 387.550）②守備的ロブ（00-57-56 292.300）③黄ロブの低い打点（01-57-16 245.100） |
| 通常（明瞭なリング＋色つきトレイル） | 8 / 11 | |

→ 「**ショット種別のエフェクトが無い打点**」は **約 3/11 ≒ 27%**。
→ 「**打点の印（リング）すら無い打点**」は **0/11**（今回の範囲では見つからなかった）。
→ 共通点: **3件とも「ボールが地面すれすれの低い打点」**。
  つまり **地面すれすれの打点＝バウンドと最も紛らわしい状況で、まさにエフェクトが弱くなる**。これが本課題の芯。

### C-2. 弱いエフェクトになる条件（実測から）

1. **とびつき（伏臥スライディング）** — 実例 2
2. **守備的な低い返球／ロブ** — 実例 6・8
3. **遠側（画面上部）の打点は絶対サイズが小さい** — 実例 4（クレイ赤 遠側 y=120）
   リング最大半径は ≈40〜50px にしかならない。**ROI 半径を y から決めないと外れる**

### C-3. 未検証の「出ないかもしれない」ケース（→未確認節）

空振り／ネットイン（ネットコード）／フレームショット（芯を外した打球）／スマッシュ／
サーブ（専用カメラのため別扱いが必要）は、今回**実例を特定できなかった**。

---

## D. バウンド側の見た目（コート別）

### D-1. 実測した実バウンド 3件

| # | 動画 | 時刻 | コート | 入射 | 見えたもの |
|---|---|---|---|---|---|
| B1 | 10-44-35 | **387.217** | 砂 | ドロップ（低速） | **無**。ボールが影に接するだけ |
| B2 | 10-44-35 | **385.183** | 砂 | 青スライス（水色トレイルつき・中速） | **無**。水色のトレイル柱が上空に残るが、着地点には何も出ない |
| B3 | 00-39-45 | **502.383** | ウッド（船の木甲板） | 橙トレイルつき（高速） | **無**。橙トレイルが着地点を通過するだけ |

**3件すべてで、砂埃・水しぶき・白いリング・ショット色の星形バーストのいずれも出ない。**
バウンド痕（マーク）も残らない。

### D-2. 打点とバウンドの見分け（本題の答え）

| | 打点 | バウンド |
|---|---|---|
| 円環 | **必ず出る**（11/11）。打点位置に固定して拡大 | **出ない**（3/3） |
| 明るい新規画素 | 打点中心から同心円状 | ボール本体とトレイルのみ |
| 位置 | ラケット高さ（地上 0.3〜1.6m 相当）・キャラの至近 | 地面（影と重なる） |
| ボール影 | ボールから離れたまま（低い打点では接近するが最小値が浅い） | dy が V字の谷（ball-bounce.md A-2） |

→ **リング＝打点の十分条件に近い**（誤検出源は B-4 のトレイル／チャージオーラ）。
→ **リング無し＝バウンド、とは断言しない方がよい**（弱い打点が 3/11 ある）。
  **「リング検出」を打点側の強い正例に使い、幾何（接地高さ・影・最寄りキャラ距離）を主判定に据える**のが安全。

### D-3. ★x 速度の反転（安価な補助判定・実測）

鎖の終端の前後で **x 速度がどう変わるか**を実測した。

| イベント | 直前 dx/frame | 直後 dx/frame | 判定 |
|---|---|---|---|
| 打点 22-18-37 302.100（低い返球） | **−1.2** | **+11.4** | **符号反転** |
| 打点 10-44-35 386.400 | −0.1 | −5.0 | 同符号だが大きさ 50倍 |
| 打点 22-02-04 118.583 | −0.3 | −27 | 同符号だが大きさ 90倍 |
| **バウンド 10-44-35 387.217** | −7.0 | −0.6 | 同符号・**減速のみ**（ドロップで転がりに移行） |
| **バウンド 00-39-45 502.383** | **+4.6** | **+4.8** | **ほぼ不変** |

→ **バウンドは x 速度がほぼ保存される**（ウッドで +4.6→+4.8）。
→ **打点は x 速度が符号反転するか、桁で変わる**。
→ ただしドロップの着弾（砂 387.217）は摩擦で x が急減速するので、
  **「x が大きく変わった＝打点」だけでは誤判定する**。リング検出と併用すること。

---

## E. 実装できる検出器の仕様

前提: ボール追跡の鎖の終端 `(t0, P=(px,py))`（FHD）が既知。960x540 で処理（ball.js と同じ）。

### E-0. まず参照フレーム差分は使わないこと（実測の教訓）

「t0−9フレームの画像と比べて明るくなった画素」を数える方式を実装して 4例で試した結果:

| 方式 | 砂 打点 | 砂 バウンド | **ウッド バウンド** |
|---|---|---|---|
| 単純差分（>28L） | 88% | 22% | **100%（誤検出）** |
| 参照を ±4px の局所最大に緩和 | 54% | 22% | **100%（誤検出）** |
| ＋トレイル方向±32°をマスク | 69% | 36% | **100%（誤検出）** |

原因は**カメラのドリー**。00-39-45 の 502.2〜502.8 では**画面が 5px/frame（FHD）動いており、9フレームで約 50px**。
ウッドコートは明暗の板目が交互に並ぶので、50px ずれるだけで広範囲が「明るくなった」と判定される。
（court-robustness の「0.2秒で3px以下」はラリー静止時の値で、**ラリー中は成立しない**。）

→ **同一フレーム内で完結する検出器にする。** 以下が実測で通った方式。

### E-1. リング検出器（`ring4.js` 実装済・reference-free）

```
入力: フレーム画像 I(t)、打点候補 P=(px,py)（FHD）
d      = 0.0155 * (py + 651)              // その深さでのボール直径（FHD px）
Rmax   = clamp(5.5 * d, 60, 260)          // 探索する最大半径

// 1) 局所コート基準（同一フレーム内）
base = P を中心に ±1.3*Rmax の正方格子を 6px 刻みでサンプルし、
       中心から 60px 以内は除外した集合の **輝度85パーセンタイル**
       （※中央値ではダメ。ウッドの明暗板目で誤検出する。実測で median→P85 に変えて
          ウッドのバウンド 100% → 25% に落ちた）

// 2) 円形カバレッジ
for r in 10..Rmax step 5:
    72方向に等角サンプル（楕円補正: dx = r*cosθ, dy = r*sinθ*(H/W比))
    cov(r) = (L > base + 30 である方向の割合)
bestCov = max_r cov(r),  bestR = argmax
```

**実測値（-0.15秒〜+0.40秒を 1/60 秒刻みで走らせた最大 bestCov）**

| ケース | 打点前の bestCov | **ピーク bestCov** | ピーク時刻 | ピーク半径 |
|---|---|---|---|---|
| 砂 打点（ドロップ） | ≤11% | **43%** | t0+0.150 | r=30 |
| 砂 **とびつき**打点 | 26〜36%（ラケット/キャラ） | **68%** | t0+0.017 | r=10 |
| ウッド ボレー打点 | ≤26% | **100%** | t0−0.04〜+0.10 | r=10〜30 |
| **砂 バウンド** | ≤18% | **21%** | — | r=90〜130（＝外周ノイズ） |
| **ウッド バウンド** | ≤21% | **25%** | — | r=10〜90（ばらつく） |

**判定ルール（推奨）**

```
isImpact(t0,P) =
   ∃ t ∈ [t0-0.03, t0+0.25]:
       bestCov(t) >= 0.38                                   // 主条件
   AND bestR が 3フレーム以上にわたり **単調に外へ**動く      // 円環が広がっている
   AND bestR(peak) <= 5.5*d                                  // 大きすぎるものは背景
   AND cov(r) が半径方向に **ピークを持つ**（r±30 で 15pt 以上落ちる）
                                                             // トレイル／板目は単調減衰なのでここで落ちる
```

- しきい 0.38 は 実測 打点 43/68/100% vs バウンド 21/25% の間。**余裕は狭い（43 vs 25）**。
- 余裕を広げる改良（未検証）: 無彩色画素には緩い閾値を使う
  `L > base+30 || (S < 0.20 && L > base+12)`。砂コートは base(P85)=198 に対し
  リングが L=228 しかないので 30 では際どい（一方コートラインが L=229 なので単純に下げられない）。

**実装上の注意**
- **base のサンプル窓は、リングが大きいと自分自身を含んでしまう**。
  ウッドのボレー例では base が 91 → 153 に押し上げられた（リング直径が窓に収まったため）。
  → **内側除外半径を 1.2*Rmax まで広げる**か、**コート多角形内・ライン画素を除いた集合**から取ること
  （shot-color.md J の L90 の取り方と同じ流儀）。
- **72方向すべてを見る必要はない**。トレイル方向（入射・出射の ±32°）を落としても
  打点の cov はほとんど下がらなかった（砂 打点 88%→69%）。ただし**ウッドの誤検出は消えなかった**ので、
  マスクは補助であって主対策ではない。

### E-2. 補助シグナル

**(a) マックスチャージ完了フラッシュ（打点の予告・±0.25秒）**
```
pureWhite(t) = count(L > 238 && max-min < 26)  // 960x540、4px 間引き
flash = pureWhite(t) > 2.0 * median(pureWhite over ±1s)
```
実測 2.1〜2.7倍で 5例すべて検出。**打点そのものではない**（−0.25〜+0.15秒のズレ）ので、
「この付近に強打がある」という**探索窓のヒント**にのみ使う。

**(b) 白い振り抜き弧**
打点の −0.05〜+0.05秒に出る細長い三日月。**円形性が無い**ので E-1 の円形テストでは拾えない。
「打点が近い」ことの弱いヒント止まり。単独では使わない。

**(c) トレイル尾端**
shot-color.md の方式で打点位置を取れるが、**とびつき・弱い返球ではトレイルが出ない**（3/11）。
必ず E-1 と併用すること。

### E-3. 全体の推奨（打点／バウンドの判別フロー）

```
鎖の終端イベント e(t0, P) について:

1. 幾何（主判定・ball-bounce.md の資産をそのまま使う）
   - 影とのdy が V字の谷 → バウンド寄り
   - コート平面へ投影した高さ ≒ 0 → バウンド寄り
   - 最寄りキャラ（player-position.md）まで < 1.2 * キャラ幅 → 打点寄り
2. **x 速度の変化（D-3）**
   - 符号反転 → **打点で確定**
   - |dx| がほぼ不変（±20%以内）→ **バウンド寄り**
3. リング検出（E-1）を t0-0.03 〜 t0+0.25 で回す
   - bestCov >= 0.38 かつ半径が外へ広がる → **打点で確定**（バウンド 3例では最大 25%）
   - 出なかった場合は 1.+2. の判断を採用（弱い打点を落とす可能性が 3/11 残る）
4. 白フラッシュが t0±0.25 にあれば「強打の可能性」フラグ（球種決定には使わない）
```

**時間窓のまとめ（打点 t0 基準・すべて実測）**

| 事象 | 窓 |
|---|---|
| ラケット振り抜き弧 | −0.05 〜 +0.05 |
| **インパクトリング出現** | **+0.017 〜 +0.05** |
| リング最大 | +0.13 〜 +0.20 |
| リング消滅 | +0.25 〜 +0.37 |
| ショット色トレイル | +0.02 〜 +0.50 |
| マックスチャージ白フラッシュ | **−0.25 〜 +0.15**（打点マーカー不可） |

→ **サンプリングは 4フレーム（0.067秒）間隔で足りる**（リングの寿命が最短でも 15フレーム）。
  ただし**とびつきの細環は 5フレームしか見やすい時間がない**ので、
  **打点候補の周辺だけは 1フレーム刻み**で見ること。

---

## 座標・数値まとめ（1920x1080 基準）

- ボール直径 `d = 0.0155 * (y + 651)`（camera-homography F5 の消失点 yVP=−651 由来）
- インパクトリング最大半径 `Rmax ≈ (3.5〜5.5) * d`
  - 実測: y=120 → r≈40 ／ y=235 → r≈65 ／ y=496 → r≈100 ／ y=800 → r≈85（とびつきは拡大が遅く +0.38秒でまだ拡大中）
- リング拡大速度: 通常打 **6〜10 px/frame** ／ とびつき型 **3〜4 px/frame**
- とびつき環の画素値: **RGB (228,228,228) / L=228 / S=0.00**、線幅 **≈19px**、直径 **≈160px**
- 砂コート面: RGB (219,203,100) L=197 S=0.54 ／ コートライン RGB (238,225,223) L=229 S=0.06
- pureWhite（960空間・4px間引き）のコート別ベースライン: 砂 320 ／ クレイ赤 640〜800 ／ ウッド 405 ／ 石畳 330 ／ 青ハード 250〜500
- **カメラのドリー速度: 最大 5 px/frame（FHD）**（00-39-45 t=502.2〜502.8 で実測）

---

## 未確認

1. **空振り・ネットイン（ネットコード）・フレームショット（芯を外した打球）・スマッシュ**の実例を
   特定できなかった。これらでリングが出るか／出ないかは**未確認**。
   特に**ネットに当たって落ちる球**は、鎖の終端がネット上に来るため打点・バウンドどちらでもない
   第3のイベントになるはず。要追加調査。
2. **サーブの打点**。サーブは専用カメラ（10-44-35 t≈382.3 で確認）で画角が大きく変わり、
   カメラも高速に振れる。今回のリング検出器がそのまま通るかは**未確認**。
3. **曇天の芝 `23-19-09`（影が一切出ないコート）でのバウンド**を実例で確認できなかった。
   影が無いコートでは幾何側の主判定が弱くなるため、リング検出への依存度が上がる。要確認。
4. **クレイ赤／青ハード／石畳／芝のバウンド**は実例を特定できなかった（確認できたのは砂 x2・ウッド x1）。
   「バウンドは無エフェクト」の結論は**3件・2コート**の根拠であることに注意。
   クレイ赤で「バウンドらしき鎖の終端」を1件追ったが（22-18-37 t=302.100）、
   **x速度も反転していたため打点だった**（実例11）。低い打点はバウンドと見分けがつきにくい。
5. リングが**打点位置に固定**という結論は、22-02-04（118.583 / 119.717）・01-57-16（304.363）・
   00-57-56（290.883）で明瞭に確認した。10-44-35 386.400 のケースだけは**中心が打点より約40px 左**にあり、
   完全な固定かどうかは**測り切れていない**。
6. E-1 の改良案（無彩色画素に緩い閾値）は**未検証**。
7. 「エフェクト無しの打点 0/11」は**今回見た 11例での値**。母数が小さい。
   特に C-2 の「地面すれすれの打点」は 3/11 と高頻度なので、実運用での取りこぼし率は別途測るべき。

---

## 再現に使った素材

- タイル画像（作業ディレクトリ）
  - `gt_tile_1/31/61/91/121.png` — 正解実例 386.0〜388.2秒の全景 60fps
  - `g2_tile.png` — 386.10〜386.50（チャージ完了フラッシュ→キャンセル）
  - `g3_tile.png` — 386.35〜386.57 を 2倍拡大（接触フレームの特定）
  - `gesso_01_06 / 07_12 / 13_18.png` — 打点直後のリング成長
  - `bz_tile.png` — 砂バウンド 387.15〜387.33 を 2倍拡大（無エフェクトの証拠）
  - `hz_tile.png` / `hz2_tile.png` — とびつき打点 387.50〜387.68 の 1.5倍/2倍拡大
  - `q1_tile.png` — 砂バウンド 385.10〜385.28（水色トレイルつき・無エフェクト）
  - `w1_tile.png` / `wd_tile.png` — ウッドバウンド 502.30〜502.48（無エフェクト・カメラドリー）
  - `c1_tile.png` / `c2_tile.png` — クレイ赤 近側マックスチャージ／遠側マゼンタ
  - `b1_tile.png` / `b2_tile.png` — 青ハード水色／石畳橙
  - `u1_tile.png` — 青ハード 守備的ロブ（細環のみ）
  - `p1_tile.png` — 石畳 黄ロブ（弱い環）
  - `s2_tile.png` — 船甲板 ボレー
  - `cy_tile.png` / `cw_tile.png` — クレイ赤 地面すれすれの返球（実例11・バウンドと紛らわしい打点）
- スクリプト: `an.js`（追跡＋pureWhite）／ `roi.js`（ROI統計）／ `ring2.js` `ring3.js`（参照差分方式・却下）／ **`ring4.js`（採用方式）**
