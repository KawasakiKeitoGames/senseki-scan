# 打点／バウンド判別器の設計と検証

調査日: 2026-08-25 / 担当: discriminator（v3 rally probe）
根拠フレームはすべて ffmpeg で抜き出し、Read ツールで**実際に目視**して確認したもの。

## 判定

**条件付きで可能。エフェクトを一切使わずに判別できる。**

- 決め手は **「打点ではボールのコート奥行き方向(Z)が反転し、バウンドでは反転しない」** というテニスの物理。
  これを地面平面ホモグラフィの**見かけZ**で測ると、高さ h の分の系統バイアスが乗るが、
  そのバイアスは `B(Z) = (c0 + c1·Z)/K` という**閉じた式で書けて実測で検証済み**なので差し引ける。
- 人手ラベル **12イベント**（打点8・バウンド4／2動画・砂とクレイ赤）で検証:
  - 正解時刻を与えた場合 → **判定できた11件は11件とも正解**（残り1件は追跡がボールを再捕捉できず UNKNOWN）。
    打点 qc = 0.252〜1.078 / バウンド qc = −0.222〜+0.008。**しきい値 0.15 で分離、マージン約1.6倍**。
  - **追跡から通しで走らせても12件中11件の種別が正解**（§8-2）。時刻誤差 0.03〜0.12秒（1件だけ0.29秒）。
- **ただし偽イベントが多い**（22秒で13件）。原因は判別器ではなく**ラリー画のシーン判定が無いこと**と
  **チャージオーラ／打撃バーストの塊が鎖になること**。前段の整備が必須。
- **とびつき（エフェクト無し）でも動く**。実例 t=387.55 のハナチャンのとびつきは qc=0.349 で正しく打点判定。
- **白い閃光（マックスチャージ）は打点マーカーとして使えない**ことを2例で確認（下記 §2）。
- **影(ball↔shadow の dy)は 960x540 では再現できなかった**（§5）。主判定に使ってはいけない。
- 未解決: **奥コートのバウンドの実例が1件も採れなかった**（§7）。式は左右対称なので理屈上は動くが未検証。

---

## 1. ユーザー提供の正解実例（10-44-35 / 386.0〜388.2）の再検証

### 1-1. 追跡パイプラインの再現性

`tools/ball.js` と同じアルゴリズム（isBall → CC → DP鎖 → pickBall → ballSegments）を Node に移植し、
`2026-08-20 10-44-35.mp4` を 960x540 で通した（作業ファイル `probe.js` / `track.js`）。

- 最上位の鎖: **t=386.217〜388.083（82点・rank=118.9）**
- **ユーザー提供の座標と完全一致**:
  - t=386.783 → 画面FHD **(696,333)**（ユーザー値 696,332）
  - t=387.217 → 画面FHD **(454,812)**（ユーザー値 454,812）
- カメラ推定（このウィンドウの中央値）: `c0=8.9428e-4, c1=2.6121e-5, Xc=-0.560, Yc=10.704`
  - t=387.217 → **court=(-3.31, -5.21)**。ユーザー値 (-3.31,-5.14) と一致。
  - t=386.783 → **court=(-3.11, +4.29)**。ユーザー値 (-2.61,+5.67) とは差がある。
    **カメラはドリーするので、どのフレームの cam を使うかで Z が 1m 前後ずれる**。
    座標を比べるときは cam を明示すること。

### 1-2. ★重要な訂正: ゲッソーの実際のラケット接触は t≈386.42。386.80 ではない

386.38〜386.70 を60fps・2倍拡大で18コマ全部を目視（作業ファイル `d_tile.png`）:

| t | 見えたもの |
|---|---|
| 386.380〜386.413 | **ボールがラケットのガット上に乗っている**（引いた姿勢→振り出し） |
| **386.430** | ボールの位置に**白い円環**が発生。ゲッソーは振り抜き後 |
| 386.447〜386.530 | 白い円環が拡大（時計状のディスク） |
| 386.547〜386.647 | 円環の中にボールが見え、**薄紫のスパイク**が四方に出る |
| 386.663〜 | ボールが左下へゆっくり動き出す（ドロップショット） |

→ **接触は t=386.42±0.02**。386.80 はそこから 0.37 秒後で、ボールはすでにゲッソーから
   画面上 55px 左・120px 下を自由飛行中（同フレームでゲッソーは FHD(761,204)、ボールは (696,333)）。
→ 本調査の鎖は 386.217〜388.083 が**繋がっており 386.80 に切れ目は無い**。
   **「鎖の終端＝打点」という前提は成立しないケースがある**: ドロップショットはボールが遅すぎて
   鎖が切れずに打点をまたいで繋がる。逆に強打では打点の前後 0.2〜0.3秒が丸ごと欠測する（§6）。

### 1-3. このシーンの実測タイムライン（FHD座標）

| t | 画面(x,y) | court(X,Z) | 実際に起きたこと |
|---|---|---|---|
| 385.50〜385.75 | (692,622)→(769,386) | Z=-6.13→+0.54 | ハナチャンの打球が奥へ |
| 386.15〜386.30 | — | — | **白い4条星フラッシュ**（マックスチャージ完了） |
| 386.22〜386.40 | (791,285)→(781,235) | Z=+5.13→+7.71 | ボールがゲッソーへ接近（画面y 単調減少） |
| **386.42** | (781,235)付近 | Z=+7.71 | **★打点**（ラケット接触・目視確定） |
| 386.43〜386.66 | — | — | 白い円環→紫スパイク。ボールはほぼ静止 |
| 386.58〜387.20 | (769,252)→(459,790) | Z=+7.08→-4.66 | ドロップショットが手前コートへ降下 |
| **387.217** | **(454,812)** | **(-3.31,-5.21)** | **★バウンド**（画面y 極大） |
| 387.23〜387.52 | (450,815)→(475,854) | Z≈-5.3 | 低く弱いバウンド後・ほぼ転がる |
| **387.55** | (475,854) | Z=-5.34 | **★とびつき打点**（エフェクト無し・目視確定） |

---

## 2. ★白フラッシュは打点の目印にならない（2例で確認）

| 例 | 白い4条星の時刻 | 実際のラケット接触 | ずれ |
|---|---|---|---|
| 10-44-35 ゲッソー | **386.15〜386.30** | **386.42** | 星は接触の **0.27〜0.12秒前** |
| 22-02-04 ゲッソー | **118.567〜118.60** | **118.56** | 星は接触と**同時** |

→ **白い4条星と接触のずれは 0〜0.27 秒でバラバラ**。ユーザーの指摘どおり
   「白フラッシュ＝マックスチャージ完了の合図」であって、打つかどうか・いつ打つかとは独立している。
   （クレイの例が「完了フラッシュ」なのか「インパクト閃光」なのかは本調査では区別していない＝未確認。
   いずれにせよ**時間オフセットが安定しないので打点マーカーには使えない**。）
   **`ball.js` の `events()` にある `nearFlash → kind:'hit'`（±0.09秒窓）は誤り**:
   砂の例（ずれ0.12〜0.27秒）は窓から外れて打点を取りこぼし、
   逆にチャージだけしてキャンセルした場合は打点でない位置に hit を作る。
   また `Tracker.isFlash` によるフレーム破棄は、チャージ完了フラッシュのたびに
   **打点と無関係な位置で 7〜10 フレームの欠測を作る**ので、判別器はその欠測を前提に設計する必要がある。

**打撃エフェクトの実観測（打点側の副産物・詳細は impact-effects 担当へ）**:
接触フレームで**ボールの位置に白い円環が生まれ、8〜12フレームかけて拡大し、ショット色の星型バーストに変わる**。
砂 383.85(橙) / 384.50(青) / 386.43(紫) / クレイ 118.60(青) / 119.72(桃) / 122.33(黄) の6例すべてで同じ挙動。
**バウンドではこの円環は出ない**（砂 389.23 のバウンドは煙も輪も出ずボールが跳ねるだけ）。
ただし**とびつき(387.55)では小さな白い輪しか出ず色バーストは無い**ので、エフェクト単独では取りこぼす。

---

## 3. 正解データ（人手ラベル・12イベント）

すべて60fpsの拡大タイルを Read ツールで目視して判定した。作業タイルは §9 に一覧。

| # | 動画 | t | ラベル | 側 | 画面(x,y) | 見かけZ | 目視の根拠 |
|---|---|---|---|---|---|---|---|
| 1 | 10-44-35 | 383.79 | **打点** | 手前 | (591,676) | -2.90 | ラケットがボールに当たる→橙バースト（`evA_tile`） |
| 2 | 10-44-35 | 384.48 | **打点** | 奥 | (1164,211) | +11.78 | ゲッソーの青バースト・ボールは影から70px上＝空中（`evB_tile`/`evK_tile`） |
| 3 | 10-44-35 | 385.19 | **バウンド** | 手前 | (605,825) | -8.99 | ボールが影に接触して跳ねる（`evCD_tile` 上段） |
| 4 | 10-44-35 | 385.39 | **打点** | 手前 | (640,826) | -9.94 | ボールがガット上→白い輪（`evCD_tile` 下段） |
| 5 | 10-44-35 | 386.42 | **打点** | 奥 | (781,235) | +7.71 | ボールがガット上→白環→紫（`d_tile`） |
| 6 | 10-44-35 | 387.22 | **バウンド** | 手前 | (454,812) | -5.21 | ユーザー提供の正解・画面y極大 |
| 7 | 10-44-35 | 387.55 | **打点** | 手前 | (475,854) | -5.34 | **とびつき**。ラケットが地面のボールを掬う。色バースト無し（`evG_tile`） |
| 8 | 10-44-35 | 389.23 | **バウンド** | 手前 | (510,924) | -10.94 | ボールが影に接触して跳ね上がる（`evH_tile`） |
| 9 | 22-02-04 | 117.25 | **バウンド** | 手前 | (1331,932) | -9.65 | 全画面で確認。ボールの周囲に選手がいない（`P1w_tile`） |
| 10 | 22-02-04 | 118.56 | **打点** | 手前 | (1272,711) | -9.92 | 接触と同時に白い4条星→青バースト（`P2_tile`） |
| 11 | 22-02-04 | 119.70 | **打点** | 奥 | (619,110) | +13.91 | ノコノコがガット上に保持→桃バースト（`P3_tile`） |
| 12 | 22-02-04 | 122.30 | **打点** | 手前 | (1330,510) | -2.43 | ラケットが当たる→白い振り抜き弧→黄バースト（`P4_tile`） |

参考（本調査では未再測・`ball-bounce.md` の確定例）:
`00-57-56` t=328.90 サンボのノーバウンド打点（手前・ボレー）。鎖の終端は t=328.767 で **打点の0.13秒前**。

内訳: **打点8 / バウンド4**。すべて手前側イベントが8、奥側が3（バウンドは全部手前）。

---

## 4. エフェクトに依存しない手掛かりの実測評価

### 4-1. ★本命: 「見かけZの離脱速度」（高さバイアス補正つき）

**原理**
テニスでは打点でボールのZ方向の進行が反転し、バウンドでは反転しない。
ところが地面平面ホモグラフィが返す**見かけZ**は、ボールが高さ h にあると

```
Z_app = Z_true + h · B(Z),     B(Z) = (c0 + c1·Z) / K        [m / m]
```

だけ**カメラから遠い側（+Z側）へずれる**。イベント直後は打点でもバウンドでも必ず dh/dt>0 なので、
見かけZ の速度に **+B·ḣ** のバイアスが乗る。手前側(Z<0)ではこのバイアスが打点と同じ向き、
奥側(Z>0)では逆向きに効く。**だから素の見かけZ速度では手前と奥で符号規約が食い違う。**

`B(Z)` の実測検証（10-44-35 t=386.40・打点フレーム）:
ボールの見かけ Z=8.98、**同フレームのゲッソーの足元** FHD(852,259) の Z=7.83 → 差 **+1.15 m**。
式は `B(7.83)=3.93` なので、ラケット高 h=0.29 m と読める。ゲッソーは背が低く、
目視でもボールは足元から30cm程度の低い位置にあった。**式と実測が0.05m以内で合う。**

**特徴量**

```
s   = sign(Z_evt)                       // +1=奥, -1=手前
dZ  = イベント後 0.03〜0.50 秒の見かけZ の最小二乗傾き [m/frame]
q   = -s · dZ                           // 「相手側へ進んだ速さ」
qc  = q + s · B(Z_evt) · ḣ ,  ḣ = 0.07 m/frame (≒4 m/s)   ← 高さバイアスの補正
判定: qc > 0.15 → 打点 / qc <= 0.15 → バウンド
```

**実測結果（12イベント）**

| ラベル | t | 側 | 見かけZ | 標本数 | dZ/frame | q | B | **qc** | 判定 |
|---|---|---|---|---|---|---|---|---|---|
| 打点 | 383.79 | 手前 | -2.90 | 0 | — | — | 2.93 | — | **UNKNOWN** |
| 打点 | 384.48 | 奥 | +11.78 | 23 | -0.453 | 0.453 | 4.30 | **0.754** | 打点 ✅ |
| バウンド | 385.19 | 手前 | -8.99 | 3 | -0.057 | -0.057 | 2.36 | **-0.222** | バウンド ✅ |
| 打点 | 385.39 | 手前 | -9.94 | 15 | +0.465 | 0.465 | 2.27 | **0.306** | 打点 ✅ |
| 打点 | 386.42 | 奥 | +7.71 | 21 | -0.223 | 0.223 | 3.92 | **0.498** | 打点 ✅ |
| バウンド | 387.22 | 手前 | -5.21 | 15 | -0.015 | -0.015 | 2.71 | **-0.205** | バウンド ✅ |
| 打点 | 387.55 | 手前 | -5.34 | 12 | +0.538 | 0.538 | 2.70 | **0.349** | 打点 ✅ **(とびつき)** |
| バウンド | 389.23 | 手前 | -10.94 | 22 | +0.108 | 0.108 | 2.18 | **-0.044** | バウンド ✅ |
| バウンド | 117.25 | 手前 | -9.65 | 21 | +0.169 | 0.169 | 2.30 | **+0.008** | バウンド ✅ |
| 打点 | 118.56 | 手前 | -9.92 | 17 | +0.411 | 0.411 | 2.27 | **0.252** | 打点 ✅ |
| 打点 | 119.70 | 奥 | +13.91 | 9 | -0.754 | 0.754 | 4.63 | **1.078** | 打点 ✅ |
| 打点 | 122.30 | 手前 | -2.43 | 16 | +0.547 | 0.547 | 3.01 | **0.336** | 打点 ✅ |

→ **判定できた11件は11件正解（100%）**。打点の最小 0.252 / バウンドの最大 +0.008 → **しきい値0.15で余裕がある**。
→ UNKNOWN の1件（383.79）は判別器の失敗ではなく**追跡がイベント後にボールを再捕捉できなかった**ため（§6）。

**補正しない素の q だと分離しない**ことを確認: バウンドの q は -0.057/-0.015/+0.108/+0.169、
打点の q は 0.223/0.411/0.453/0.465/0.538/0.547/0.754 → 0.169 と 0.223 の間しか空かず、
**しかも奥側の打点(0.223)が手前側のバウンド(0.169)に迫る**。B 補正で初めて 0.008 vs 0.252 に開く。

### 4-2. 画面y速度（vIn / vOut）— 手前側だけなら効くが奥側で破綻

FHD px/frame での実測:

| ラベル | 側 | vIn（イベント前） | vOut（イベント後） |
|---|---|---|---|
| バウンド 385.19 | 手前 | **+29.8** | -0.6 |
| バウンド 387.22 | 手前 | **+26.1** | +3.3 |
| バウンド 389.23 | 手前 | **+25.5** | -11.2 |
| バウンド 117.25 | 手前 | **+38.4** | -15.2 |
| 打点 385.39 | 手前 | 0.0 | **-25.2** |
| 打点 387.55 | 手前 | +1.0 | **-24.2** |
| 打点 122.30 | 手前 | +19.8 | **-29.8** |
| 打点 383.79 | 手前 | +21.4 | (-30 相当) |
| 打点 386.42 | **奥** | **-5.7** | +3〜+12 |
| 打点 384.48 | **奥** | (欠測) | **+0.4** |
| 打点 119.70 | **奥** | -5.7 | +13.1 |

- **手前側**: バウンドは vOut が -15〜+3（ほぼ止まる）、打点は vOut が -24〜-30。**|vOut| ≥ 20 px/f で分離できる。**
  幾何的な理由: 手前バウンド直後は「上昇による画面下向き成分」と「カメラへ接近する画面下向き成分」が
  ほぼ相殺する。打点直後は「上昇」と「遠ざかる」が両方 画面上向きに効いて足し算になる。
- **奥側**: 打点の vOut がわずか +0.4〜+13 px/f しかない（遠近圧縮で1px≒0.05m）。
  **奥側では画面px速度は使えない。必ず見かけZ（メートル）に直すこと。**
- vIn は打点・バウンドどちらも「自陣ベースライン方向へ接近」なので**判別力は無い**（入射脚は同一）。

### 4-3. 側の交替（構造的制約）— HMMの遷移として使える

砂ラリーの確定列: `打点(手前) → 打点(奥) → バウンド(手前) → 打点(手前) → 打点(奥) → バウンド(手前) → 打点(手前) → バウンド(手前)`
クレイ: `バウンド(手前) → 打点(手前) → 打点(奥) → … → 打点(手前)`

確認できた規則（12イベントすべてで成立）:
1. **打点は必ず側が交替する**（手前→奥→手前…）。実測 5連続で成立。
2. **バウンドは必ず「次に打つ側」で起きる**。奥の打点の次のバウンドは手前、逆も同様。
3. **連続する2イベントが同じ側なら、その並びは (バウンド, 打点) の順**。
4. **打点と打点の間のバウンドは0個か1個**（0個＝ボレー）。実測でボレーが多い（12件中3件が打点→打点直行）。

時間間隔（参考・分離力は弱い）:
- 打点→バウンド: 0.71 / 0.80 s
- バウンド→打点: 0.20 / 0.33 / 1.31 s
- 打点→打点（ボレー）: 0.69 / 1.03 / 1.14 s
→ **「バウンド→打点は短い」は成り立たない**（高く弾んだ 117.25 は 1.31 s）。順序制約だけ使うこと。

### 4-4. 見かけZの絶対値・イン/アウト判定

- バウンドの見かけZ = 真のZ（h=0）なので **`inCourt()` がそのまま使える**。
  実測 4バウンド: Z=-8.99 / -5.21 / -10.94 / -9.65、|X| は 3.3〜3.5 → すべてコート内で妥当。
- 打点の見かけZ は h·B(Z) だけ奥へずれる。奥側では B≒4 なので **ラケット高0.9mで約3.6m の誤差**。
  実測 119.70 の打点は見かけ Z=+13.91 で**ベースライン(11.885)より奥に出る**。
  → **|Z_app| > 12.5 の終端は「バウンドではない」と断言してよい**（アウトのバウンドは別途扱い）。
- 逆に |Z_app| < 12.5 でも打点はありうる（384.48 は +11.78、386.42 は +7.71）。単独では使えない。

### 4-5. 「相手側に到達したか」（ネット越え判定）

イベント後、次のイベントまでの間に見かけZが 0 を越えて相手側に入るか:
- 打点 8件: 383.79→+9.96 / 384.48→-7.5 / 385.39→+0.54 / 386.42→-3.0 / 387.55→+18.9 / 118.56→+4.8 / 122.30→+18.0 …**7件で越える**
- バウンド 4件: -9.89 / -5.34 / -7.81 / -3.53 …**1件も越えない**
- 例外 119.70（奥の打点）は追跡が Z=+0.4 で切れて越えを確認できず。

→ **確度の高い裏取りにはなるが、必ず「次のイベントまで」で打ち切ること**。
  打ち切らないと 385.19 のバウンド(0.20秒後に打点)が「越えた」と誤判定する（実測で踏んだ）。

---

## 5. ★影(dy)は 960x540 では再現できなかった

`ball-bounce.md` の推奨方式（局所最大クリップ + chromaDist≤0.08）を 960x540 で実装し
（作業ファイル `shadow.js`）、砂コートの正解イベントで dy を測った結果:

| イベント | 正解 | 検出された dy (FHD) |
|---|---|---|
| 386.400 打点（空中・高さ約0.3m） | 打点 | **4.1** |
| 386.350 打点直前 | — | 4.5 |
| 385.183 バウンド直前 | バウンド | 13.9 |
| 385.200 バウンド | バウンド | 11.3 |
| **387.217 バウンド** | バウンド | **検出なし** |
| 387.100 飛行中（高い） | — | 6.0 |
| **389.23 バウンド** | バウンド | **検出なし（389.18〜389.35 全滅）** |
| 383.75 飛行中 | — | 151.3（別物を掴んでいる） |

→ **打点(4.1) < バウンド(11.3) となり順序が逆転**。`ball-bounce.md` の落とし穴14
   （発光体の近くの正常なコート面が偽の影になる）が 960x540 では消えていない。
   バウンド本番のフレームは砂埃と反射でむしろ影が消える。
→ **結論: 影は主判定にも裏取りにも使わない。** 使うなら 1920x1080 ネイティブで、
   かつ「ボール直下 8〜14px の影は偽物として捨てる」ガードを足してからにすること。

---

## 6. イベント時刻・位置の推定（終端は打点そのものではない）

鎖の終端と実際のイベントのずれ（実測）:

| イベント | 直前の最終点 | 直後の最初の点 | ずれ |
|---|---|---|---|
| 386.42 打点(ドロップ) | 386.400 | 386.583 | -0.02 / +0.16 |
| 387.22 バウンド | 387.217（そのまま） | 387.233 | **0** |
| 385.19 バウンド | 385.183 | 385.200 | **0** |
| 383.79 打点 | 383.767 | （再捕捉失敗） | -0.02 / — |
| **384.48 打点（強打）** | **384.217** | **384.550** | **-0.26 / +0.07** |
| 328.90 打点（`00-57-56`） | 328.767 | 329.050 | **-0.13 / +0.15** |
| 118.56 打点 | 118.550 | 118.700 | -0.01 / +0.14 |

→ **バウンドは終端がほぼそのままイベント位置**。
→ **打点は強打ほど前後が欠測する**（最大0.26秒＝16フレーム）。エフェクトとオクルージョンのため。
→ 対策: 前後の鎖を**それぞれ1次でフィットして交点を取る**。
  **交点は「傾きの変化が大きい方の軸」で解く**こと（傾き差 < 60 px/s の軸は不定なので使わない）。

実測精度（±0.30秒の窓で前後3点以上ある場合）:

| 正解 t | 精密化の出力 | 採用軸・傾き差 | 誤差 |
|---|---|---|---|
| 386.42 打点 | **386.414** | y / 506 px/s | 0.006 |
| 387.22 バウンド | **387.229** | y / 1240 px/s | 0.009 |
| 385.19 バウンド | **385.217** | y / 1990 px/s | 0.027 |
| 385.39 打点 | **385.346** | y / 1754 px/s | 0.044 |
| 389.23 バウンド | **389.205** | y / 2677 px/s | 0.025 |
| 384.48 打点 | **算出不能**（前脚に点が無い） | — | — |

→ **±0.045秒（3フレーム）以内**。ただし 384.48 のように前後どちらかの脚が丸ごと欠測すると出せない。
  その場合は終端の時刻をそのまま使い、`kind` は判定できても**時刻は最大0.26秒ずれる**と割り切る。
  この例では画面yがほぼ変化せず（215→186）画面xだけが +7 px/f → -14 px/f と反転するので、
  前脚さえ取れれば x 軸で解ける（y軸では不定になる）。**両軸を試す実装にすること。**

---

## 7. 未確認・弱点

- **奥コートのバウンドの実例を1件も採れなかった**（10-44-35 の1ラリー・22-02-04 の2ラリー・
  11-33-43 / 00-57-56 / 301〜305 のサーブ前後を見たが、奥側は全部ボレー／スマッシュだった）。
  式は s で対称化してあるので理屈上は動くが、**奥側バウンドの qc は未検証**。
  幾何モデルからの予測: 奥バウンド直後は dZ_true>0（さらに奥へ）＋ B·ḣ>0 の両方が +Z に効くので
  `q = -s·dZ` は **強い負（-0.4 前後）**になり、`qc = q + B·ḣ ≒ -0.4+0.28 = -0.12` → バウンド判定に入る。
  奥側の打点（実測 qc = 0.498 / 0.754 / 1.078）とは十分離れる見込み。
- **11-33-43（芝・FHD）で追跡が破綻**した（196〜200秒。上位の鎖が背景の装飾物に乗った）。
  芝は `isBall` の g≥160 を背景が満たしやすい。**判別器以前に追跡の課題**。
- ダブルス（`00-39-45`）・フィーバーショット中は未検証。
- **`ball.js` の鎖抽出に取りこぼしがある**（下記）。判別器の入力品質を直接下げる。
- 高さ ḣ=0.07 m/frame は固定値。強打のバウンドで ḣ が 0.12 を超えると qc が 0.15 を跨ぐ恐れがある
  （実測の最大は 117.25 の高いバウンドで qc=+0.008 なのでまだ余裕はある）。

### ★ ball.js の鎖抽出の取りこぼし（実測・要修正）

`buildChains` の貪欲な取り出しで
```js
for (let nd = end; nd; nd = nd.prev) { if (nd.used) { path.length = 0; break; } path.push(nd); }
```
は、**DPの祖先が既に使われた瞬間に鎖を丸ごと捨てる**。
実測: 10-44-35 の t=383.90〜384.22（20点・打点直後の正しいボール軌跡）がこれで消え、
383.79 の打点が UNKNOWN になった。`path.length = 0` をやめて **その場で打ち切る**（`break` のみ）だけで
383.85〜384.00 の鎖が復活する。ただし今度は**橙バーストの塊（20x17px, n=195）が鎖になって混ざる**ので、
併せて「イベント直後の候補は入射脚との速度連続性でゲートする」対策が要る。

---

## 8. 推奨アルゴリズム（`tools/ball.js` の `bounces()` 置き換え）

現状の `bounces()` は「降下したまま終わった鎖の終端」を**すべて** `segment-end` として出している。
以下はそれを **打点/バウンド/不明** の3値に分類する差し替え版。**エフェクトを一切参照しない。**

```js
  // ---- 打点／バウンド判別 ----
  // 原理（docs/rally-probe/discriminator.md）:
  //   テニスでは「打点で奥行きZの進行が反転し、バウンドでは反転しない」。
  //   地面平面ホモグラフィが返す見かけZは高さhの分だけ +Z 側にずれる:
  //       Z_app = Z_true + h * B(Z),   B(Z) = (c0 + c1*Z) / K        ← 実測で0.05m以内に一致
  //   イベント直後は打点もバウンドも dh/dt>0 なので、見かけZ速度に +B*ḣ のバイアスが乗る。
  //   側 s(+1=奥/-1=手前) で符号を揃え、このバイアスを引くと 12件中 判定できた11件が11件正解。
  //   しきい値 0.15 m/frame。打点 0.252〜1.078 / バウンド -0.222〜+0.008。
  const HDOT = 0.07;          // イベント直後の典型的な上昇速度 [m/frame] ≒ 4 m/s
  const QTHRESH = 0.15;       // qc のしきい値 [m/frame]

  // 見かけZの「高さ1mあたりの押し出し量」。court.js の cam から閉じた式で出る。
  function zBias(Z, cam) { return (cam.c0 + cam.c1 * Z) / 0.2796e-3; }

  // segments（ballSegments の出力）を1本の時系列に畳む
  function mergeTrack(segments) {
    const pts = [];
    segments.forEach((s, si) => s.pts.forEach(p => pts.push({ ...p, seg: si })));
    return pts.sort((a, b) => a.t - b.t);
  }

  // 直線フィット（Z vs frame）。返り値 m/frame
  function slopeZ(seq) {
    const n = seq.length; if (n < 3) return null;
    let sf = 0, sz = 0, sff = 0, sfz = 0;
    for (const p of seq) { sf += p.f; sz += p.Z; sff += p.f * p.f; sfz += p.f * p.Z; }
    const d = n * sff - sf * sf;
    return Math.abs(d) < 1e-9 ? null : (n * sfz - sf * sz) / d;
  }

  // イベント候補の抽出（★画面yの反転では足りない）
  //  実測: 弱いバウンド(387.22)は画面yが反転せず +26→+2 px/f と減速するだけ。
  //        地面のボールを掬うとびつき(387.55)も反転しない。奥側の打点は画面yがほとんど動かない。
  //  → **見かけZ速度の折れ（kink）**を主候補にする。鎖の端は補助。
  //  パラメータは実測で調整済み: W=6フレーム / kink>=0.18 m/frame / 非極大抑制 ±3標本 / 統合 0.12秒
  function eventCandidates(segments, track) {
    const out = [];
    for (const s of segments) {
      const p = s.pts; if (p.length < 4) continue;
      out.push({ t: p[p.length - 1].t, x: p[p.length - 1].x, y: p[p.length - 1].y, src: 'seg-end' });
      out.push({ t: p[0].t, x: p[0].x, y: p[0].y, src: 'seg-start' });
    }
    const W = 6;
    const sl = (a, b) => {                      // 最小二乗 Z vs frame
      let n = 0, sf = 0, sz = 0, sff = 0, sfz = 0;
      for (let i = a; i <= b; i++) { const p = track[i]; if (!p || !isFinite(p.Z)) continue; n++; sf += p.f; sz += p.Z; sff += p.f * p.f; sfz += p.f * p.Z; }
      if (n < 4) return null;
      const d = n * sff - sf * sf; return Math.abs(d) < 1e-9 ? null : (n * sfz - sf * sz) / d;
    };
    const kink = new Array(track.length).fill(0);
    for (let i = W; i < track.length - W; i++) {
      if (track[i + W].f - track[i - W].f > 3 * W) continue;    // 欠測が多すぎる区間は使わない
      const A = sl(i - W, i - 1), B = sl(i + 1, i + W);
      if (A != null && B != null) kink[i] = Math.abs(B - A);
    }
    for (let i = W; i < track.length - W; i++) {
      if (kink[i] < 0.18) continue;
      let isMax = true;
      for (let k = -3; k <= 3; k++) if (kink[i + k] > kink[i]) { isMax = false; break; }
      if (isMax) out.push({ t: track[i].t, x: track[i].x, y: track[i].y, src: 'kink' });
    }
    out.sort((a, b) => a.t - b.t);
    const merged = [];
    for (const e of out) {
      const last = merged[merged.length - 1];
      if (last && e.t - last.t < 0.12) {                        // 二重計上を潰す。kink を優先
        if (last.src !== 'kink' && e.src === 'kink') merged[merged.length - 1] = e;
        continue;
      }
      merged.push(e);
    }
    return merged;
  }

  // 打点時刻の精密化: 前後の鎖を1次でフィットして交点を取る。
  // **向きの変化が大きい軸で解く**こと（実測: 強打は画面yがほぼ動かず画面xだけ反転する）。
  function refineTime(track, e) {
    const before = track.filter(p => p.t < e.t && p.t >= e.t - 0.14);
    const after  = track.filter(p => p.t > e.t && p.t <= e.t + 0.30);
    if (before.length < 3 || after.length < 3) return e.t;
    // 独立変数は t（秒）そのもの。傾き a は px/秒
    const lin = (seq, key) => {
      const n = seq.length; let st = 0, sv = 0, stt = 0, stv = 0;
      for (const p of seq) { st += p.t; sv += p[key]; stt += p.t * p.t; stv += p.t * p[key]; }
      const d = n * stt - st * st; if (Math.abs(d) < 1e-9) return null;
      const a = (n * stv - st * sv) / d;
      return { a, b: (sv - a * st) / n };
    };
    let best = null;
    for (const key of ['x', 'y']) {
      const A = lin(before, key), B = lin(after, key);
      if (!A || !B) continue;
      const dv = Math.abs(A.a - B.a);
      if (dv < 60) continue;                    // 傾きが実質変わらない軸は使わない (px/s)
      const tX = (B.b - A.b) / (A.a - B.a);
      if (!best || dv > best.dv) best = { dv, t: tX };
    }
    if (!best) return e.t;
    return (best.t > e.t - 0.30 && best.t < e.t + 0.30) ? best.t : e.t;
  }

  // ---- 本体 ----
  // segments: ballSegments の出力（pts に t,f,x,y[960空間] を持つ）
  // camAt(t): その時刻の Court.estimate 結果
  function classifyEvents(segments, camAt, opts = {}) {
    const margin = opts.margin ?? 0.6;
    const qth = opts.qThresh ?? QTHRESH;
    const track = mergeTrack(segments).map(p => {
      const cam = typeof camAt === 'function' ? camAt(p.t) : camAt;
      const c = cam && cam.ok ? Court.toCourt(p.x * SC, p.y * SC, cam) : { X: NaN, Z: NaN };
      return { ...p, X: c.X, Z: c.Z, cam };
    });
    const cands = eventCandidates(segments, track);
    const out = [];
    for (let i = 0; i < cands.length; i++) {
      const e = cands[i];
      const tEvt = refineTime(track, e);
      const at = track.reduce((b, p) => (!b || Math.abs(p.t - tEvt) < Math.abs(b.t - tEvt) ? p : b), null);
      if (!at || !at.cam || !at.cam.ok) { out.push({ t: tEvt, x: e.x, y: e.y, kind: 'unknown' }); continue; }
      const s = at.Z >= 0 ? 1 : -1;
      // 次のイベントの手前で打ち切る（打ち切らないとバウンド直後の打点を拾って誤判定する）
      const tStop = Math.min(tEvt + 0.50, (cands[i + 1] ? cands[i + 1].t : Infinity) - 0.06);
      // 入射脚との連続性ゲート（バーストの塊を掴まないため）: 40px/frame + 40px @FHD
      const gate = p => {
        const df = Math.max(1, (p.t - tEvt) * 60);
        return Math.hypot((p.x - at.x) * SC, (p.y - at.y) * SC) <= 40 * df + 40;
      };
      let seq = track.filter(p => p.t >= tEvt + 0.03 && p.t <= tStop && gate(p));
      if (seq.length >= 3 && seq[seq.length - 1].t - seq[0].t < 0.08)
        seq = track.filter(p => p.t >= tEvt + 0.03 && p.t <= Math.min(tEvt + 0.7, tStop) && gate(p));
      const dZ = slopeZ(seq);
      let kind = 'unknown', qc = null;
      if (dZ != null && seq.length >= 3) {
        qc = (-s * dZ) + s * zBias(at.Z, at.cam) * HDOT;
        kind = qc > qth ? 'hit' : 'bounce';
      }
      // 幾何の常識で上書き: 見かけZが |12.5| を超える点は地面ではありえない
      if (kind === 'bounce' && Math.abs(at.Z) > 12.5) kind = 'hit';
      out.push({
        t: tEvt, x: at.x, y: at.y, kind, qc: qc == null ? null : +qc.toFixed(3),
        side: s > 0 ? 'opp' : 'me',
        X: +at.X.toFixed(2), Z: +at.Z.toFixed(2),
        inCourt: Court.inCourt(at.X, at.Z, margin),
      });
    }
    return applyRallyRules(out);
  }

  // 構造制約で後始末（12イベントすべてで成立を確認した規則）
  //  1. 打点は側が交替する          2. バウンドは「次に打つ側」で起きる
  //  3. 同じ側の連続2イベントは (バウンド, 打点) の順
  //  4. 打点と打点の間のバウンドは0個か1個
  function applyRallyRules(evs) {
    for (let i = 0; i < evs.length; i++) {
      const a = evs[i], b = evs[i + 1];
      if (a.kind === 'unknown' && b && b.kind === 'hit' && b.side === a.side) a.kind = 'bounce';   // 規則3
      if (a.kind === 'unknown' && b && b.kind === 'bounce' && b.side !== a.side) a.kind = 'hit';   // 規則2
      if (a.kind === 'bounce' && b && b.kind === 'bounce' && a.side !== b.side) a.kind = 'hit';    // 規則2の逆
    }
    // 打点の側が2回続けて同じなら、その間に取りこぼした打点があるので警告フラグ
    let prevHit = null;
    for (const e of evs) {
      if (e.kind !== 'hit') continue;
      if (prevHit && prevHit.side === e.side) e.suspect = 'same-side-hits';
      prevHit = e;
    }
    return evs;
  }
```

**呼び出し側の変更**: 既存の `bounces(segments, cam)` は
`classifyEvents(segments, camAt).filter(e => e.kind === 'bounce')` に置き換えられる。
打点列は `.filter(e => e.kind === 'hit')` で同時に取れる。

**併せて直すべき3点**
1. `events()` の `nearFlash ? 'hit' : 'bounce'` を撤去する（§2 で誤りと確定）。
2. `buildChains` の `path.length = 0; break;` を `break;` にして鎖の取りこぼしを止める（§7）。
3. `events()` の「画面yの極大」だけでは弱いバウンドととびつき打点を取りこぼす。
   候補抽出は上記の **見かけZ速度の折れ（kink）** に置き換える。

### 8-2. 通しで動かした結果（end-to-end）

上の実装をそのまま Node に落として（作業ファイル `classify2.js`）、
追跡→鎖→候補抽出→判別まで通しで走らせた結果。

**砂 10-44-35 t=383.5〜390（正解8件）**

| 出力 t | 判定 | qc | 側 | 見かけZ | 正解 | 可否 |
|---|---|---|---|---|---|---|
| 383.697 | bounce | -0.358 | 手前 | -0.87 | 打点 383.79 | ✗（鎖が打点直後にバーストの塊へ乗り移った） |
| 384.771 | hit | 1.033 | 奥 | +8.99 | 打点 384.48 | ✅ 種別は正解・**時刻が0.29秒遅い** |
| 385.224 | **bounce** | -0.233 | 手前 | -9.22 | バウンド 385.19 | ✅ |
| 385.336 | **hit** | 0.312 | 手前 | -9.66 | 打点 385.39 | ✅ |
| 386.397 | **hit** | 0.184 | 奥 | +7.71 | 打点 386.42 | ✅ |
| 387.236 | **bounce** | -0.187 | 手前 | -5.26 | バウンド 387.22 | ✅ |
| 387.486 | **hit** | 0.365 | 手前 | -5.32 | 打点 387.55（とびつき） | ✅ |
| 389.204 | **bounce** | -0.047 | 手前 | -11.05 | バウンド 389.23 | ✅ |

**クレイ 22-02-04 t=117〜122.5（正解4件）**

| 出力 t | 判定 | qc | 正解 | 可否 |
|---|---|---|---|---|
| 117.219 | **bounce** | 0.026 | バウンド 117.25 | ✅ |
| 118.444 | **hit** | 0.304 | 打点 118.56 | ✅ |
| 119.702 | **hit** | 1.072 | 打点 119.70（奥） | ✅ 時刻も一致 |
| 122.276 | **hit** | 0.429 | 打点 122.30 | ✅ |

→ **通しでも12件中11件の種別が正解**。時刻誤差は 0.03〜0.12秒（384.48だけ0.29秒）。

**ただし偽イベントが多い**: 上記2ウィンドウ（計22秒）で正解8+4件に対し
**偽イベント13件・`unknown` 6件**が出た。内訳はほぼ全部
(a) ラリー外（ポイント間・リプレイ・サーブ準備）の背景ブロブの鎖、
(b) チャージオーラの塊が作る鎖（`ball-bounce.md` 落とし穴16）、
(c) 打撃バーストの塊が作る短い鎖。
→ **前段に `player-position` Step 0 のシーン判定（ラリー画だけ通す）が必須**。
   加えて鎖の採用条件に「n が期待面積の1.8倍を超える塊は除外」「入射脚との速度連続性」を足すこと。
→ `unknown` はイベント後にボールを再捕捉できなかったケース。**握りつぶさず unknown のまま出す**のが安全。

---

## 9. 作業ファイル

`C:/Users/iftec/AppData/Local/Temp/claude/C--Users-iftec-Documents-Claude/61bc9629-efb3-474e-aaa8-2e0d1cbe769e/scratchpad/h-discriminator/`

| ファイル | 内容 |
|---|---|
| `probe.js` | rgb24 960x540 ストリーム → 毎フレームの {カメラ推定, ボール候補, フレーム統計} JSONL |
| `track.js` / `track2.js` | JSONL → DP鎖 → pickBall → ballSegments → コート座標つきトラック表 |
| `shadow.js` | 局所最大クリップ方式の影検出（§5 の再現失敗の実験） |
| `eval2.js` | 12イベントの qc 特徴量評価（§4-1 の表を生成） |
| `classify.js` / **`classify2.js`** | **§8 の提案アルゴリズムの通し実装（`classify2.js` が調整済み・§8-2 の表を生成）** |
| `r1.jsonl/.txt` | 10-44-35 t=380〜392（正解実例を含むラリー） |
| `r5.jsonl/.txt` | 22-02-04 t=116〜126（クレイ赤） |
| `r7.jsonl/.txt` | 22-02-04 t=125.4〜129.4 |
| `d_tile.png` | **386.38〜386.66 の2倍拡大18コマ。ゲッソーの接触が見える最重要タイル** |
| `evA/evB/evCD/evG/evH/evK_tile.png` | 砂ラリーの各イベントの60fps拡大タイル |
| `P1/P2/P3/P4_tile.png`, `P1w_tile.png` | クレイ赤の各イベント |
| `a_tile.png` | 386.2〜387.4 の全体俯瞰（15fps 19コマ） |

使い方:
```
ffmpeg -hide_banner -loglevel error -ss 380 -t 12 -i "…10-44-35.mp4" \
  -f rawvideo -pix_fmt rgb24 -vf scale=960:540 - | node probe.js 380 60 > r1.jsonl
node track.js r1.jsonl 28 > r1.txt
node eval2.js
```
