# ボール追跡とバウンド判定（ストローク/ボレー） — v3（完成）

## 判定

**検出可能だが要工夫。**

- **ボール検出（色）は9コート中の実測7コートすべてで成立**。r/g・b/g 判定はコート面・キャラ（サンボ含む）を確実にはじく。
- **ボレー（バウンド前打球）の実例を60fps連番で確定**（`00-57-56` 青ハード t=328.40〜328.98）。
  打点でのボール↔影の距離 **dy=132px(720p)=198px(FHD)**、バウンド時の **dy=18px(FHD)** に対し **約10倍**。
  → **ストローク/ボレーの判別は映像から可能**。
- ただし **影に依存する設計は不可**（曇天芝 `23-19-09` は影が一切出ない）。
  影なしでも成立する主判定として **「相手の打点から自分の打点までの区間にバウンド（y反転）が無ければボレー」**
  を採用し、dy は裏取りに使う。
- **ハーフボレー（バウンド直後の低い打球）とストロークの分離は不可**（実測 dy 33px vs バウンド 18px で差が小さい）。

## 判明した事実（v2 から継続 + v3 追加）

### A. ボールと影
1. **ボールの影は地面に描画される**（砂・芝・ウッド・**船甲板**・**青ハード**・**石畳**で確認。
   例外は曇天の `23-19-09` で、ここだけ影が一切出ない）。
   ボールが高いほど影は画面下方に離れ、接地で重なる。
   - 根拠: `2026-08-20 10-44-35.mp4` t=386.5〜387.5 の60fps連番（f38〜f53）を
     x=330..630 / y=700..960 で拡大したタイル。降下→接触→再上昇が一望できる。
   - **実測バウンド = t≈387.20s**
2. dy（shadow.cy - ball.cy）の実測系列（同シーン, FHD px）:
   f30..f42 = 105,98,101,96,85,87,66,61,53,46,41,29,**18** → f43-45欠測 → f46..f55 = 26,31,34,38,41,44,46,47,49,50
   → **きれいなV字**。谷=バウンド。dy=0にはならない（最小18px）ので「極小」で判定する。
   （v3で 960x540 の自作トラッカで再現。dy 最小 13px@960 = 26px@FHD、時刻 t=387.18〜387.27 で一致）
3. 影の輝度は周囲比 **0.32〜0.70**（コート依存・下表）。芝・ウッドの方がコントラストが強い。

#### A-2. ★v3実測: コート別のボール影コントラスト

| コート | 動画/時刻 | ボール影の有無 | 影 L | 周囲コート L | 比 |
|---|---|---|---|---|---|
| 砂 | 10-44-35 t=387.1〜387.6 | **◎ 明瞭**（60fps拡大タイルで全コマ視認） | — | — | キャラ影 0.62〜0.67 |
| 芝 | 11-33-43 | ◎（既報） | — | — | 高コントラスト |
| 芝・起伏 | 23-19-09 t=300 | **× 影が一切出ない**（既報・決定的反例） | — | — | — |
| **船・木甲板** | 00-57-56 t=113.00 (x=860-871 縦プロファイル) | **◎◎ 最強** | **20**（y=572） | **63**（y=585） | **0.32** |
| **青ハード** | 00-57-56 t=328.90 (850,572) | ○ 小さい暗楕円（29x9px@720p） | **69** RGB(51,64,147) | **104** RGB(86,98,185) | **0.66** |
| **クレイ赤** | 22-18-37 t=300.6〜301.5 | ボール影は未撮／**キャラ影は左下に明瞭** | — | — | — |
| **石畳** | 01-57-16 t=244.933 (806-818, 462-470) | **◎ 明瞭**（60fps拡大タイル `stb_tile.png` で全コマ視認） | **20〜30** | **60〜70** | **≈0.38** |

**石畳の2次元輝度マップ実測**（`01-57-16` t=244.933、x=730..890 / y=390..510 を 4x4 平均、値は L/10）:
```
 ...
y=462  7766678566666556664222345666778766555555   ← 列19-22 (x=806..818) が L=20〜40
y=466  7666679566655556666423356666678866575555
y=470  8667768566666666679978877777779966676556
```
→ 周囲の石畳 L=60〜70 に対し影の核 L=20〜30。**石畳でもボール影ははっきり出る**。

**★光源方向はコートごとに違う（影の探索範囲に効く）**

| コート | ボールに対する影のオフセット | dx/dy |
|---|---|---|
| 砂 `10-44-35` | 左下（光源=右上） | 負・小 |
| **石畳** `01-57-16` t=244.933 | **右下** ball(775,413) → shadow(812,466) @720p | **+0.70** |
| 船甲板 `00-57-56` t=113.00 | ほぼ真下（dx≈0） | ≈0 |
| 青ハード `00-57-56` t=328.90 | ほぼ真下 ball(863,448) → shadow(858,580) | −0.04 |

→ **影の探索窓は「真下」では足りない**。`|dx| ≤ 0.8·dy + 20K` を推奨（石畳の 0.70 を包含）。

**船甲板の縦プロファイル実測**（`00-57-56` t=113.00、x=860..871 の12px平均）:
```
y=546  (176,190, 82) L=173   ← ボール本体
y=555  (104,102, 22) L= 94   ← ボール下端
y=566  ( 96, 48, 37) L= 61   ← 影の入り口
y=572  ( 38, 13, 11) L= 20   ← 影の核（最暗）
y=580  ( 94, 46, 36) L= 59   ← 影の出口
y=585+ (102, 48, 37) L= 63   ← 甲板の地色
```
→ **ボール影は幅12px以上・高さ約14px、L比 0.32。キャラの影よりずっと濃い。**
→ 影の彩度も変わる: 甲板の正規化クロマ (0.549,0.258) に対し影は (0.613,0.210)、**chromaDist=0.080**。
   **court-robustness の「影 = chromaDist≤0.030」はボール影には狭すぎる**（キャラの半影用の値）。0.08 まで許容が必要。

### B. ボールの見え方
4. 色（v3で全コート実測。6x6〜10x10平均、`isBall` は g≥140 && 0.84<r/g<1.02 && 0.28<b/g<0.68）:

| コート | 動画/時刻 | ボール RGB | r/g | b/g | isBall | コート面 RGB | r/g | isBall |
|---|---|---|---|---|---|---|---|---|
| 砂 | 10-44-35 t=306.35 (1027,201) | (199,204,132) | 0.972 | **0.646** | ✅ | (216,193,95) | 1.121 | ❌ |
| **クレイ赤** | 22-02-04 t=121.23 (1163,145) | (194,197,113) | 0.984 | 0.575 | ✅ | (197,124,125) | 1.584 | ❌ |
| **クレイ赤(別動画)** | 22-18-37 t=300.56（shot-color G より） | (198,202,122) | 0.980 | 0.604 | ✅ | (183,93,95) | 1.968 | ❌ |
| **青ハード（遠）** | 00-57-56 t=328.50 (782,232) | (149,171,86) | 0.871 | 0.503 | ✅ | (74,87,179) | 0.851 / g<140 | ❌ |
| **青ハード（近）** | 00-57-56 t=328.90 (858,444) | (204,218,139) | 0.932 | 0.636 | ✅ | 〃 | | |
| **船・木甲板** | 00-57-56 t=113.00 (865,543) | (197,201,84) | 0.981 | 0.421 | ✅ | (102,48,36) | 2.125 | ❌ |
| **石畳** | 01-57-16 t=245.48 (809,281) | (176,179,84) | 0.986 | 0.469 | ✅ | (49,71,75) | 0.690 / g<140 | ❌ |

→ **`isBall` は追加4コート（クレイ赤・青ハード・船・石畳）すべてで成立**。
→ ただし **b/g は遠方・強光で 0.65 まで上がる**（砂 t=306.35 で 0.646）。**上限 0.68 は余裕が小さい**。
   推奨: 予測位置の近傍だけ **0.80 < r/g < 1.06 / 0.25 < b/g < 0.75** に緩める（下記アルゴリズム 7-c）。

5. **★v3実測: 画素サイズ（FHD 実座標で計測）**

| 位置 | 動画/時刻 | 実測 bw x bh | 画素数 n |
|---|---|---|---|
| **相手ベースライン付近**（y=204） | 10-44-35 t=306.350 | **14 x 14** | **96** |
| 相手サービスライン手前（y=213） | 10-44-35 t=306.400 | 18 x 13 | 89 |
| 中景（y=254〜284） | 10-44-35 t=386.58〜386.70 | 18x14 〜 26x21 | 198〜330（尾込み） |
| 手前（y=790〜830） | 10-44-35 t=387.1〜387.5 | 22x17 〜 34x26 | — |
| 720p 換算（青ハード y=232） | 00-57-56 t=328.50 | 12 x 8 | 51 |

→ **最小は FHD で 14x14 px（n≈96）**。1280x720 なら **9x9 px（n≈43）**、2560x1440 なら 19x19。
→ 経験式（FHD）: **ボール直径 d ≒ 0.0155 × (y + 651)**（消失点 yVP=-651 は camera-homography F5 の確定値）。
   y=204 → 13.3px、y=790 → 22.3px。実測とよく合う。**面積ゲートはこの式から作れる**。
→ **480x270 まで落とすとボールは 3.5x3.5 px になり検出不能**。処理解像度の下限は **960x540**。

6. フレーム間移動量（60fps, FHD）: 落下中で 10〜32 px/frame。
   速球は**白いコメット状の尾（モーションブラー）**を引き、ボール本体と繋がって
   1つの連結成分になる（bw/bh が伸びる）。

### C. ★v3 最重要: ボレー（バウンド前打球）の実例

**実例: `2026-08-21 00-57-56.mp4`（青ハード・1280x720）t=328.40〜328.98。**
遠側 ヨッシー（紫）が t≈328.35 に打った球を、**ネット際に詰めていた サンボ（Pokey）が
ノーバウンドで打ち返す**。60fps 全コマを拡大タイルで目視確認済み
（作業ファイル `vz_tile.png` / `vz2_tile.png`）。

**自作トラッカによるボール screen-y の実測（1280x720 ネイティブ）**:

| t | by | 状況 |
|---|---|---|
| 328.400 | 206.2 | 遠側の打球直後（青トレイル先端） |
| 328.500 | 234.2 | 降下中（**フル画で目視: ネットの上・遠コート上空**） |
| 328.600 | 269.8 | 降下中 |
| 328.700 | 312.9 | **ネット上端を通過**（この x でのネット上端 y≈310） |
| 328.800 | 388.3 | 近コート上空・降下中 |
| 328.850 | 408.8 | 〃 |
| 328.883 | 434.4 | 〃 |
| **328.900** | **447.6** | **最下点＝ラケット接触**（サンボの足元 y≈590 より 143px 高い） |
| 328.917 | 412.5 | 上昇（白い振り抜き弧＋白リング発生） |
| 328.983 | 272.5 | 上昇 |
| 329.083 | 128.3 | 遠コートへ |

**判定の根拠（3つとも独立に成立）**:
1. **単調降下**: 328.400→328.900 の 30フレーム、by は 206→448 まで**一度も反転しない**。
   ＝ 近コート側でバウンドしていない。
2. **ボール↔影の距離**: 接触フレーム t=328.900 で影を (858.3, 579.6) / 29x9px に検出、
   **dy = 132.0 px @720p = 198 px @FHD**。
   同じ手法での**バウンド時の dy 最小は 18px @FHD**（砂 t=387.20）。**約10倍**の差。
3. **目視**: 拡大タイル `vz2_tile.png`（t=328.70〜328.98 の18コマ）で、
   ボールがラケットに当たる瞬間、**コート面上にボール影の暗い小楕円がはっきり別位置に見える**。

→ **結論: ストローク/ボレーの判別は映像から可能。**

**ボレー2例目（石畳・やや弱い証拠）**: `2026-08-21 01-57-16.mp4` t=244.85〜245.07。
遠側の打球が **t≈244.90 にネットを越え、0.15秒後の t≈245.05 にピーチのラケットに達する**。
その間 60fps 全15コマ（`stb_tile.png`）でボールは単調降下し、**影は常に 40〜55px 下に離れたまま**。
→ ノーバウンド打球（ドライブボレー）と考えられるが、
   **接触フレームでボールがかなり低い（影との距離 ≈40px@720p = 60px@FHD ≈ 2.7×ボール直径）**ため
   ハーフボレーの可能性を完全には排除できない。**dy 単独では白黒つかない例**として記録する。

**対照例（ストローク側）**: 砂 `10-44-35` t=387.10〜387.60（拡大タイル `bz_tile.png` 30コマ）。
ボールが降下→**t≈387.20 で影と接触（バウンド）**→わずかに浮いて→**t≈387.55 に近側がラケットで打つ**。
この打点での dy は約 33px@FHD しかなく（バウンド 18px との差が小さい）、
**ハーフボレーとストロークの区別は dy では不可能**。バウンドイベントの有無で判断するしかない。

### D. ★v3実測: 隠蔽・欠測の頻度

砂コート `10-44-35` の1ポイント（t=304.60〜311.40、**409フレーム**）を 1920x1080 ネイティブで
通しトラッキングし、フレームごとに分類:

| 分類 | フレーム数 | 割合 |
|---|---|---|
| ボール候補ゼロ（隠蔽・オーラ汚染・フラッシュ） | 54 | **13.2%** |
| 画面端の背景を誤検出（x<40 または x>1880） | 27 | 6.6% |
| → 実質ミス | 81 | **19.8%** |
| トレイルと融合して巨大ブロブ（n>320） | 26 | 6.4% |

**連続欠測の長さ**（ラリー中）: 最長 **4フレーム（67ms）**、大半は 1〜3フレーム。
（t=311.4 以降のポイント間画面では 6〜9フレーム連続で落ちるが、これはラリーではない）

→ **6フレームまでの補間で埋めれば軌道は途切れない。**
→ 画面端 6.6% は **x ∈ [40,1880] の ROI 制限で消える**（この分は実装ミスであって隠蔽ではない）。

### E. ★v3実測: 処理コスト

計測環境 = 本PC / CapCut同梱 ffmpeg / Node v24。

| 対象 | 条件 | 実測 | 換算 |
|---|---|---|---|
| FHD 11分47秒（**42,444フレーム**） | デコードのみ `-f null -` | **28.4 s** | 1,495 fps ＝ **24.9倍速** |
| FHD 60秒 | デコードのみ | 2.31 s | 1,558 fps |
| FHD 60秒 | デコード + `scale=480:270` | 2.63 s | 1,369 fps（+14%） |
| 720p 60秒 | デコードのみ | 1.48 s | 2,431 fps ＝ **40倍速** |
| **Node 解析 @960x540** | 300フレーム（isBall CC + 影CC 込み） | 4.44 s | **≈70 fps → 42,444フレームで約10分** |
| **Node 解析 @480x270** | 300フレーム | 1.11 s | ≈300 fps → 約2.4分（ただし**ボール3.5px で検出不能**） |

**結論**:
- **デコードは律速ではない**（全フレームでも30秒）。WebCodecs なら GPU デコードでさらに速い。
- **律速は JS の画素処理**。960x540 単スレッドで **約10分/11分動画**。
- **Web Worker 4本で 2.5〜3分**が現実的な目標。
- 生RGBのパイプ量は 960x540 で 1.56MB/frame × 42,444 = 66GB。**ディスクに落とさずストリーム処理必須**
  （本調査の Node スクリプトはストリーム処理で実装した）。

### F. 重大な落とし穴（実測）
7. **ショットのオーラでボール色が汚染される**。
   `00-39-45` t=200.5 のスライス（青ビーム）では、ボール周囲が青(88,161,222)に染まり、
   ボール本体も (140,167,80)（**r/g=0.838** — 閾値 0.84 をわずかに下回る）まで転び**未検出**。
   打球直後〜オーラ消失までは固定閾値の色検出は当てにできない → 対処はアルゴリズム 7 節。
8. **ネットのメッシュが黄緑**（サンド/芝コート）。素朴な「緑っぽい」判定だと大量誤検出。
   r/g・b/g 方式ならほぼ落とせる。
9. **白いフワフワの物体**が芝・ウッド・**石畳**のコート面に静止して存在する
   （`23-19-09` t=300 に2個、`00-39-45` t=200.5 に2個、`01-57-16` t=244.6〜245.5 に1〜3個）。
   ボールではない（30フレーム連続で動かない）。**無彩色なので b/g≈1 となり `isBall` は通らない**が、
   白ベース／輝度ベースの検出を足すときは要注意。
10. 8x8 px・fill≈0.9 の小さな偽ブロブが砂コートに毎フレーム十数個出る（h264マクロブロック起因と推定）。
    面積フィルタ（FHDで n≥100）で除去可。
11. **カメラはポイント中もドリーで動く**（camera-homography F1/F6: 見かけスケールが 2倍近く変わる）。
    数秒窓の中央値背景は破綻する。**隣接フレーム差分**か**毎フレームのホモグラフィ補償**が必要。
12. **サンプル動画の解像度が3種類混在**: 1920x1080 / 2560x1440 / 1280x720（全て60fps・6Mbps）。
    しきい値は必ず K=width/1920 でスケールすること。
13. **★v3新規: 画面端の背景を誤検出する**。`10-44-35` では x≈1914 に縦長（5〜10 x 34〜56px）の
    黄緑ブロブが毎フレーム出て、ラリー409フレーム中 **27フレーム（6.6%）で最大ブロブを奪った**。
    **ROI を x ∈ [40,1880]（FHD）に制限**すれば消える。
14. **★v3新規: 「明るい物体のそばの正常なコート面」が偽の影として検出される**。
    行内移動平均（`lum < m*0.86`）方式だと、ボールの発光トレイルが局所平均を押し上げるため
    **ボールのすぐ下 7px に幽霊影が張り付く**（`00-57-56` t=328.40〜328.72 で dy=5〜10px が連続）。
    バウンド時の真の dy 最小（18px@FHD ＝ 12px@720p）と紛らわしく、**ボレーをバウンドと誤判定する**。
    → 対策: 局所背景を **上限クリップ付き（min(L, 1.25·CL)）の局所最大**で取る、
      かつ **影候補の彩度がコート面と近い（chromaDist ≤ 0.08）** ことを要求する。
15. **★v3新規: トレイル自身が `isBall` を通り、ボールと融合して巨大ブロブになる**。
    石畳 `01-57-16` t=249 では黄トレイルごと 20x16〜26x51 / n=200〜800 の連結成分になった
    （真のボールは 720p で 9x9・n≈43）。**先端抽出（アルゴリズム 2-c）が必須**。
16. **★v3新規: キャラの「チャージオーラ」が黄緑を含み `isBall` を通る**。
    クレイ赤 `22-02-04` t=121.6〜121.9 でゲッソーの虹色オーラが n=50〜120 の
    「ボール」として毎フレーム検出された（拡大確認済み・`clay_tile.png`）。
    → **キャラ bbox の内側は減点する**か、**軌道の連続性ゲート**で落とす。
17. **★v3確認済み（安全側）: サンボ（Pokey）は黄色いが `isBall` を通らない**。
    実測 頭 (215,178,66) r/g=1.211 / 胴 (202,155,39) r/g=1.307 → いずれも上限 1.02 を超える。
    「黄色い球が縦に積まれたキャラ」でも誤検出しない。

## 座標・閾値（1920x1080基準・K=width/1920でスケール）

```js
// ---- ボール色（コート非依存・7コートで実測検証済み）----
function isBall(r,g,b){
  if (g < 140) return false;
  const rg = r/g, bg = b/g;
  return rg > 0.84 && rg < 1.02 && bg > 0.28 && bg < 0.68;
}
// 予測位置の近傍(±60K px)だけ緩める版（オーラ汚染対策）
function isBallLoose(r,g,b){
  if (g < 120) return false;
  const rg = r/g, bg = b/g;
  return rg > 0.80 && rg < 1.06 && bg > 0.25 && bg < 0.75;
}

// ---- 連結成分フィルタ ----
// 期待直径 d = 0.0155 * (y + 651)   [FHD px]   → 相手BLで13、手前で22〜26
// 期待面積 A = 0.785 * d*d          → 136 〜 530
// 採用条件: 0.35*A <= n <= 3.0*A, fill > 0.42, max(bw,bh) <= 3.0*d
// 探索ROI: x=40..1880, y=0..940（左右下スコアUI / 左上吹き出し / 上部球速看板を除外）

// ---- 影（v3改訂: 局所最大クリップ方式）----
// CL = フレーム自身のコート面輝度中央値（x∈[0.15W,0.85W], y∈[0.35H,0.92H]）
// (CR,CG) = 同帯の正規化クロマ中央値
// Lc  = min(L, 1.25*CL)                       ← 発光体で局所背景が跳ねるのを防ぐ
// B   = max( 水平±34K の Lc 最大, 垂直±34K の Lc 最大 )
// 影画素: L < 0.82*B  かつ  L > 0.28*CL  かつ  chromaDist((r,g,b),(CR,CG)) <= 0.08
// CCフィルタ: 面積 0.5*A .. 2.5*A, bw > bh*1.15, fill > 0.45, bw < 100K
// ボールとのマッチ: 0 < dy < 700K かつ |shadow.cx - ball.cx| <= 0.8*dy + 20K の中で dy 最小
//   （光源方向がコート依存: 石畳は dx/dy=+0.70、砂は左下、船/青ハードはほぼ真下）
// ※ 影コントラストが弱いコート（実測比 > 0.9）では影パイプラインごと無効化する

// ---- 判別しきい値（実測値ベース）----
// バウンド:  dy_min <= 1.4 * d     （実測 18px vs d=22px → 0.8）
// ボレー:    dy_hit >= 4.0 * d     （実測 198px vs d=22px → 9.0）
// 中間(1.4d < dy < 4d) は「ハーフボレー/低い打点」として保留 → バウンド有無で決める
```

## 推奨アルゴリズム

### 全体方針
**影は補助。主判定は「軌道の反転イベント列」**。理由:
`23-19-09`（曇天の芝）は影が一切出ず、クレイ赤ではボール影の確証が取れていない。
一方で **ボール本体の色検出は7コートで100%成立** し、**y反転の検出は影なしでできる**。

### Step 0 — 前処理
1. WebCodecs `VideoDecoder` で全フレームをデコード。**960x540 の RGBA** にダウンスケール（K=0.5）。
   - 実測: デコードは 42,444フレームで 28秒（律速でない）。
   - 解析は Web Worker 4本に分割（1本 70fps → 4本で 280fps → 11分動画を 2.5〜3分）。
   - **480x270 は不可**（ボールが 3.5px になる）。
2. シーン判定（player-position Step 0）でラリー画のみ通す。ポイント間・リプレイ・カットイン除外。
3. フレームごとに `CL, CR, CG`（コート面輝度中央値・クロマ中央値）を算出。
4. 白線マスク → 毎フレームのホモグラフィ（camera-homography: `xVP=960, yVP=-651, K=2.796e-4`、
   横線2本＋縦線1本で3自由度を解く）。ネット上端テープは使わない。

### Step 1 — ボール候補の抽出（毎フレーム）
a. `isBall` マスク → 8近傍CC。ROI `x∈[40,1880], y∈[0,940]`（FHD換算、Kスケール）。
b. 各CCについて、その重心 y から期待直径 `d = 0.0155*(y+651)` と期待面積 `A` を出し、
   `0.35A ≤ n ≤ 3.0A`, `fill > 0.42`, `max(bw,bh) ≤ 3.0d` で篩う。
c. **先端抽出（トレイル融合対策）**: `max(bw,bh) > 2.2*min(bw,bh)` または `n > 1.8A` のCCは
   トレイルと融合しているとみなし、
   - 直前フレームの速度ベクトル **v** が既知なら、CC画素を **v** に射影して **95パーセンタイル点**を先端とする。
   - **v** が未知なら、CC内で `|r/g - 0.95|` が最小の画素塊（＝最もボール色に近い部分）の重心を採る。
     （実測: トレイルは 青 r/g=0.67 / 紫 2.34 / 赤 1.58 / 黄 1.06 / 白 1.00±0.03 で、
      **どの色のトレイルも r/g が 0.84〜1.02 の外にある**。ボールだけがこの帯に入る）
d. **フラッシュ検出**: フレーム平均Lが直前より25%以上跳ねる、または `L>225` 画素率が3%を超えたら
   そのフレームを `flash` とマークし、ボール候補を採用しない（shot-color D: 白フラッシュは7〜10フレーム）。

### Step 2 — トラッキング
a. 直前の位置と速度から予測位置 `p̂` を作り、ゲート **60K px + 12K px × 欠測フレーム数** 内の
   候補を最近傍で採用（実測の飛行中移動量は 10〜32 px/frame @FHD）。
b. ゲート内に `isBall` 候補が無ければ、**`p̂` の周囲 60K px だけ `isBallLoose` で再探索**（オーラ汚染対策）。
c. それでも無ければ欠測。**連続6フレーム（100ms）まで**は3次スプラインで補間して軌道を繋ぐ
   （実測: ラリー中の最長欠測は4フレーム）。7フレーム以上空いたらトラックを打ち切る。
d. キャラ bbox の内側で見つかった候補は **スコアを半減**（チャージオーラ誤検出の抑制、落とし穴16）。

### Step 3 — 影の検出とマッチング（コート適性がある場合のみ）
a. **コート適性の自動判定**: そのコートの最初の数百フレームで、検出済みキャラの足元直下に
   `L/CL` の谷ができるかを測る。谷の比が **0.90 未満**なら影あり、**0.90 以上なら影パイプラインを無効化**
   （`23-19-09` はここで無効化される）。
b. 影マスク = 上記「局所最大クリップ方式」。**行内移動平均は使わない**（落とし穴14）。
c. ボールの下方 `y∈(by, by+700K]`、横は `|dx| ≤ 0.8·dy + 20K` の扇形で、
   面積 `0.5A..2.5A` かつ `bw>1.15bh` の最も dy が小さいCCを採用 → `dy`。
   **光源方向はコートごとに固定**（石畳=右下 dx/dy=+0.70、砂=左下、船/青ハード=ほぼ真下）なので、
   最初の数十イベントで `dx/dy` の中央値を学習して扇形を片側に絞ると誤対応が減る。

### Step 4 — イベント（打点／バウンド）の検出
a. ボールの screen-y 系列を5フレーム中央値で平滑化し、**極大点（下向き→上向きの反転）**を列挙。
   併せて速度の折れ `|Δv| > 8 px/frame（FHD）` も候補にする。
b. 各反転点を分類:
   - **打点(hit)**: 反転位置が **キャラ bbox から 0.9×キャラ高さ以内**、
     または ±5フレーム以内に **白い振り抜き弧／インパクトリング／新しいトレイルの尾端** が現れる
     （player-position F8-1,2 / shot-color B）。
   - **バウンド(bounce)**: キャラから離れており、かつ
     （影が使えるなら `dy` がその区間の**局所最小**で `dy ≤ 1.4d`）
     または（コート平面上に白リング→ショット色の星形バーストが出る: player-position F8-3）。
c. **打点フレームの精密化（オーラ汚染の本命対策）**:
   打点前後は必ず検出が飛ぶ（フラッシュ7〜10フレーム）ので、**打点フレームを直接見つけない**。
   入射側の軌道と反射側の軌道をそれぞれ **screen-y の2次式**でフィットし、
   **2本の交点の時刻**を打点時刻とする。実測データ（`00-57-56` t=328.4〜329.1）では
   入射 30フレーム / 反射 12フレーム がクリーンに取れており、**±1フレーム精度**で決まる。

### Step 5 — ★ストローク／ボレーの判別
打点 `H`（打った側 = S、時刻 `t_H`）ごとに、**直前の相手の打点 `H_prev` から `t_H` までの区間**を見る。

1. **その区間に「S 側のコート内でのバウンド」が検出されていれば → ストローク**。
   S 側かどうかはホモグラフィで Z の符号を見る（ネット中央 Z=0）。
2. **バウンドが1つも無ければ → ボレー**。
3. 影が使えるコートなら裏取り: `dy(t_H) ≥ 4.0d` ならボレー、`dy(t_H) ≤ 1.4d` ならストローク。
   （実測 ボレー 198px/d=22 → 9.0 ／ バウンド 18px/d=22 → 0.8。**10倍離れているので誤判定しにくい**）
4. **単調性ショートカット**（影不要・実測で単独成立）:
   `H_prev → H` の区間で screen-y が**単調増加（＝一度も反転しない）**なら **ボレー確定**。
   `00-57-56` の実例はこれだけで判定できた（30フレーム単調降下）。
5. `1.4d < dy < 4.0d` かつバウンドが打点の 10フレーム以内 → **ハーフボレー**（ストロークに含める）。
   実測でこの帯は分離できない（砂 t=387.55 の dy=33px, d=22px → 1.5）ので、
   **無理に細分せず「ストローク」に丸めるのが安全**。

### Step 6 — 出力
ラリーごとに `[時刻, 打った側, ストローク/ボレー/スマッシュ, 打点のコート座標(X,Z), ショット色種別]` の列。
バウンド列は `[時刻, コート座標(X,Z), イン/アウト]` として別に出す（ホモグラフィがあるので
バウンド位置は**コート平面上のイベント**として正確に取れる）。

### 実装順の推奨
1. ボール検出＋トラッキング（Step 1-2）だけ先に作り、ラリー中の検出率を可視化して詰める。
   **目標は「欠測 ≤ 6フレーム」**。実測ベースラインは 13.2%欠測・最長4フレーム。
2. y反転イベント検出（Step 4a）＋キャラ近接での打点分類（Step 4b 前半）。
   ここまでで**ラリーの打数・往復数**が取れる（＝ラリー長の指標が完成）。
3. Step 5-4（単調性ショートカット）でボレー判定を足す。**影は不要**。
4. 最後に影パイプライン（Step 3）を、適性のあるコート限定で裏取り用に足す。

## 未確認

- **クレイ赤での「ボール影」の直接撮影**（キャラ影が左下に明瞭に出ることは確認済み）。
  ボールが低い位置にあるコマを引けなかった。石畳は v3 で確認済み（比 0.38）。
- スマッシュ（`SMASH!` 表示が出る打球）が「バウンド前」か「バウンド後」かの統計。
  ラベル自体は緑文字で画面に出るので、**表示の有無を読む方が映像解析より確実**な可能性が高い。
- ダブルス（`00-39-45`）でボール以外に2体増えたときの隠蔽率。今回はシングルスのみ計測。
- Step 4c（2本の放物線フィットによる打点時刻推定）の精度を、他のコート・他の打球種で検証していない。
- WebCodecs 実測。今回の 70fps は Node（V8）での値で、ブラウザ Canvas/ImageData 経由だと
  ピクセル取り出しコストが追加される可能性がある。

## 作業ファイル

`C:/Users/iftec/AppData/Local/Temp/claude/C--Users-iftec-Documents-Claude/61bc9629-efb3-474e-aaa8-2e0d1cbe769e/scratchpad/g-ball-bounce/`
- `track2.js` … ストリーミング RGB24 → ボール/影トラッカ（行内移動平均・v2方式）
- `track3.js` … コート・クロマ方式の影（chromaDist≤0.030 → **狭すぎることを確認**）
- `track4.js` … 局所最大クリップ方式の影（**推奨方式のプロトタイプ**）
- `vz_tile.png` / `vz2_tile.png` … **ボレー実例**（青ハード t=328.60〜329.45 / t=328.70〜328.98）
- `bz_tile.png` … 砂のバウンド実例（t=387.10〜387.60、影との接触が全コマ見える）
- `shipz_tile.png` … 船甲板のボール＋影＋インパクトリング（t=112.95〜113.35）
- `stz_tile.png` … 石畳の近コート（キャラ影・白い静止物体・20fps）
- `stb_tile.png` … **石畳のボール影＋ボレー2例目**（t=244.90〜245.15、60fps 15コマ）
- `cz_tile.png` … クレイ赤のキャラ影（左下方向・22-18-37 t=300.6〜301.5）
- `rally_sand.tsv` … 隠蔽率計測に使った1ポイント分のトラック（450フレーム）
- `cost.txt` … ffmpeg デコード速度の実測ログ

使い方（例）:
```
ffmpeg -ss 328.35 -t 0.75 -i "...00-57-56.mp4" -f rawvideo -pix_fmt rgb24 - \
  | node track4.js 1280 720 328.35 60
```
